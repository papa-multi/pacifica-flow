"use strict";const LIVE_STALE_MS=20000;const LIVE_DELAYED_MS=8000;const RECONNECT_MAX_MS=5000;const SNAPSHOT_TIMEOUT_MS=25000;const POSITIONS_FEED_TIMEOUT_MS=8000;const POSITIONS_FEED_POLL_MS=500;const LIVE_EVENT_WINDOW_MS=5*60*1000;const DRAWER_FETCH_TIMEOUT_MS=8000;const WALLET_DETAIL_ENDPOINT_PREFIX="/api/live-trades/wallet/";if(typeof window!=="undefined"){window.__PF_LIVE_TRADE_SCRIPT_EXECUTED=true;window.__PF_LIVE_TRADE_BOOTED=true}const state={payload:null,payloadSignature:"",activeView:"positions",activeWalletGroup:"pnl",positionsSortKey:"detectedAt",positionsSortDir:"desc",walletsSortKey:"pnlAll",walletsSortDir:"desc",selection:{type:"",key:""},rows:{positions:[],wallets:[],liveActive:[]},rowSignatures:{positions:"",wallets:"",liveActive:""},search:"",filters:{wallet:"",minUsd:0,maxUsd:0,minPnl:0,maxPnl:0,freshness:"",trackedOnly:false},pagination:{positions:{page:1,pageSize:20},wallets:{page:1,pageSize:100}},stream:{source:"bootstrap",connection:"idle",paused:false,retryMs:500,eventSource:null,fallbackTimer:null,positionsTimer:null,metricsTimer:null,healthTimer:null,lastPayloadAt:0,lastSnapshotAt:0,lastHeartbeatAt:0,lastLiveEventAt:0,lastErrorAt:0,lastError:null,positionsRequestSeq:0,positionsAppliedSeq:0,positionsLastAppliedGeneratedAt:0,positionsRequestInFlight:false,positionsRefreshQueued:false},positionsSeenKeys:new Set,metrics:null,drawer:{wallet:"",loading:false,error:"",detail:null,sourceRow:null,requestId:0,abortController:null}};const VIEW_CONFIG={positions:{kicker:"Live Trade",title:"Position Feed"},wallets:{kicker:"Wallet intelligence",title:"Wallet Performance"}};function el(id){return document.getElementById(id)}function n(value,fallback=0){const parsed=Number(value);return Number.isFinite(parsed)?parsed:fallback}function fmtNum(value,digits=2){const num=n(value,NaN);if(!Number.isFinite(num))return"-";return num.toLocaleString(undefined,{minimumFractionDigits:digits,maximumFractionDigits:digits})}function fmtInt(value){return fmtNum(value,0)}function fmtUsd(value,digits=2){const num=n(value,NaN);if(!Number.isFinite(num))return"-";return`$${fmtNum(num,digits)}`}function fmtSignedUsd(value,digits=2){const num=n(value,NaN);if(!Number.isFinite(num))return"-";const abs=Math.abs(num);return`${num>0?"+":num<0?"-":""}$${fmtNum(abs,digits)}`}function fmtPct(value,digits=2){const num=n(value,NaN);if(!Number.isFinite(num))return"-";return`${num>0?"+":""}${fmtNum(num,digits)}%`}function tsMs(value){const maybe=n(value,NaN);if(Number.isFinite(maybe)&&maybe>0)return maybe;const parsed=Date.parse(String(value||""));return Number.isFinite(parsed)?parsed:0}function fmtTs(value){const ms=tsMs(value);if(!ms)return"-";return`${new Intl.DateTimeFormat("en-US",{timeZone:"UTC",year:"numeric",month:"short",day:"2-digit",hour:"2-digit",minute:"2-digit",second:"2-digit",hour12:false}).format(new Date(ms))} UTC`}function fmtDateOnly(value){const ms=tsMs(value);if(!ms)return"-";return new Intl.DateTimeFormat("en-GB",{year:"numeric",month:"2-digit",day:"2-digit"}).format(new Date(ms))}function fmtAgo(value){const ms=tsMs(value);if(!ms)return"-";const delta=Math.max(0,Date.now()-ms);if(delta<1000)return"now";const sec=Math.floor(delta/1000);if(sec<60)return`${sec}s ago`;const min=Math.floor(sec/60);if(min<60)return`${min}m ago`;const hr=Math.floor(min/60);if(hr<24)return`${hr}h ago`;const day=Math.floor(hr/24);return`${day}d ago`}function fmtDurationShort(value){const ms=n(value,NaN);if(!Number.isFinite(ms)||ms<=0)return"n/a";const sec=Math.floor(ms/1000);if(sec<60)return`${sec}s`;const min=Math.floor(sec/60);if(min<60)return`${min}m`;const hr=Math.floor(min/60);if(hr<24){const remMin=min%60;return remMin>0?`${hr}h ${remMin}m`:`${hr}h`}const day=Math.floor(hr/24);const remHr=hr%24;return remHr>0?`${day}d ${remHr}h`:`${day}d`}function safeArray(value){return Array.isArray(value)?value:[]}function asObject(value){return value&&typeof value==="object"&&!Array.isArray(value)?value:{}}function extractRows(value){if(Array.isArray(value))return value.filter(Boolean);const obj=asObject(value);if(Array.isArray(obj.items))return obj.items.filter(Boolean);if(Array.isArray(obj.rows))return obj.rows.filter(Boolean);if(Array.isArray(obj.data))return obj.data.filter(Boolean);if(Array.isArray(obj.list))return obj.list.filter(Boolean);return[]}function extractPublicTrades(payload){const primary=extractRows(payload===null||payload===void 0?void 0:payload.publicTrades);if(primary.length>0)return primary;const legacy=extractRows(payload===null||payload===void 0?void 0:payload.rows);return legacy}function extractWalletPerformance(payload){const primary=extractRows(payload===null||payload===void 0?void 0:payload.walletPerformance).filter(row=>isPositiveWalletPerformanceRow(row));if(primary.length>0)return primary;const legacy=extractRows(payload===null||payload===void 0?void 0:payload.wallets).filter(row=>isPositiveWalletPerformanceRow(row));return legacy}function resolveWalletPerformanceTotalPnlUsd(row){const safeRow=row&&typeof row==="object"?row:{};const safeAll=safeRow.all&&typeof safeRow.all==="object"?safeRow.all:{};const candidates=[safeRow.totalPnlUsd,safeRow.totalPnl,safeRow.pnlUsd,safeRow.realizedPnlUsd,safeAll.totalPnlUsd,safeAll.totalPnl,safeAll.pnlUsd,safeAll.realizedPnlUsd];for(const candidate of candidates){const value=Number(candidate);if(Number.isFinite(value))return value}return NaN}function isPositiveWalletPerformanceRow(row){const totalPnlUsd=resolveWalletPerformanceTotalPnlUsd(row);return Number.isFinite(totalPnlUsd)?totalPnlUsd>0:false}function extractPositions(payload){var _payload$summary;const primary=extractRows(payload===null||payload===void 0?void 0:payload.positions);const positionFeedMode=String((payload===null||payload===void 0?void 0:payload.positionFeedMode)||(payload===null||payload===void 0||(_payload$summary=payload.summary)===null||_payload$summary===void 0?void 0:_payload$summary.positionFeedMode)||"").trim().toLowerCase();if(positionFeedMode==="current"&&primary.length>0)return primary;const events=extractRows(payload===null||payload===void 0?void 0:payload.positionEvents);if(events.length>0)return events;return primary}function extractLiveActiveWallets(payload){const primary=extractRows(payload===null||payload===void 0?void 0:payload.liveActiveWallets);if(primary.length>0)return primary;return[]}function extractWalletPerformanceSupport(payload){var _payload$summary2;return asObject(payload===null||payload===void 0||(_payload$summary2=payload.summary)===null||_payload$summary2===void 0?void 0:_payload$summary2.walletPerformanceSupport)}function payloadPublicTradesTotal(payload){var _payload$summary3;const summaryTotal=n(payload===null||payload===void 0||(_payload$summary3=payload.summary)===null||_payload$summary3===void 0?void 0:_payload$summary3.publicTradesTotal,NaN);if(Number.isFinite(summaryTotal))return summaryTotal;const legacyTotal=n(payload===null||payload===void 0?void 0:payload.totalRows,NaN);if(Number.isFinite(legacyTotal))return legacyTotal;return extractPublicTrades(payload).length}function payloadWalletsTotal(payload){var _payload$summary4;const summaryTotal=n(payload===null||payload===void 0||(_payload$summary4=payload.summary)===null||_payload$summary4===void 0?void 0:_payload$summary4.indexedWalletsTotal,NaN);if(Number.isFinite(summaryTotal))return summaryTotal;const legacyTotal=n(payload===null||payload===void 0?void 0:payload.totalWallets,NaN);if(Number.isFinite(legacyTotal))return legacyTotal;return extractWalletPerformance(payload).length}function payloadPositionEventsTotal(payload){var _payload$summary5;const summaryTotal=n(payload===null||payload===void 0||(_payload$summary5=payload.summary)===null||_payload$summary5===void 0?void 0:_payload$summary5.positionEventsTotal,NaN);if(Number.isFinite(summaryTotal))return summaryTotal;return extractPositions(payload).length}function payloadPositionsTotal(payload){var _payload$summary6,_payload$summary7;const summaryTotal=n(payload===null||payload===void 0||(_payload$summary6=payload.summary)===null||_payload$summary6===void 0?void 0:_payload$summary6.openPositionsTotal,NaN);if(Number.isFinite(summaryTotal))return summaryTotal;const legacyTotal=n(payload===null||payload===void 0||(_payload$summary7=payload.summary)===null||_payload$summary7===void 0?void 0:_payload$summary7.publicTradesTotal,NaN);if(Number.isFinite(legacyTotal))return legacyTotal;return extractPositions(payload).length}function positionsSignature(payload){const positionRows=extractPositions(payload);const rows=positionRows.length>0?positionRows:extractPublicTrades(payload);const head=asObject(rows[0]);return[payloadPositionEventsTotal(payload),rows.length,tsMs(head.detectedAt||head.timestamp||head.lastTradeAt||head.updatedAt||head.lastUpdatedAt||0),String(head.eventType||head.historyId||head.id||head.accountId||head.symbol||"")].join(":")}function walletsSignature(payload){const rows=extractWalletPerformance(payload);const sample=rows.slice(0,16).map(row=>{var _ref,_item$totalVolumeUsd;const item=asObject(row);const all=asObject(item.all||item.d24||item.d30||{});return[normalizeWallet(item.wallet||item.walletAddress||""),tsMs(all.lastTrade||all.last_trade||item.lastTradeAt||item.lastUpdatedAt||item.updatedAt||item.timestamp||0),n(item.totalTrades,0),n((_ref=(_item$totalVolumeUsd=item.totalVolumeUsd)!==null&&_item$totalVolumeUsd!==void 0?_item$totalVolumeUsd:item.totalVolume)!==null&&_ref!==void 0?_ref:item.volumeUsd,0)].join("~")}).join("|");return[payloadWalletsTotal(payload),rows.length,sample].join(":")}function liveActiveSignature(payload){var _payload$summary8,_payload$summary9;const rows=extractLiveActiveWallets(payload);const sample=rows.slice(0,12).map(row=>{const item=asObject(row);return[normalizeWallet(item.wallet||""),n(item.openPositions,0),n(item.positionUsd,0),tsMs(item.lastActivityAt||item.lastPositionAt||item.liveScannedAt||0)].join("~")}).join("|");const total=Number.isFinite(n(payload===null||payload===void 0||(_payload$summary8=payload.summary)===null||_payload$summary8===void 0?void 0:_payload$summary8.liveActiveWalletsTotal,NaN))?n(payload===null||payload===void 0||(_payload$summary9=payload.summary)===null||_payload$summary9===void 0?void 0:_payload$summary9.liveActiveWalletsTotal,rows.length):rows.length;return[total,rows.length,sample].join(":")}function escapeHtml(value){return String(value==null?"":value).replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;").replaceAll("\"","&quot;").replaceAll("'","&#39;")}function normalizeSide(side){const raw=String(side||"").trim().toLowerCase();if(!raw)return"";if(raw==="bid"||raw==="buy"||raw==="long"||raw==="open_long")return"open_long";if(raw==="ask"||raw==="sell"||raw==="short"||raw==="open_short")return"open_short";if(raw==="close_long")return"close_long";if(raw==="close_short")return"close_short";return raw}function normalizeWallet(value){return String(value||"").trim()}function shortWallet(value,head=6,tail=6){const wallet=normalizeWallet(value);if(!wallet)return"";if(wallet.length<=head+tail+1)return wallet;return`${wallet.slice(0,head)}...${wallet.slice(-tail)}`}function shortTx(value,head=8,tail=8){const tx=String(value||"").trim();if(!tx)return"";if(tx.length<=head+tail+1)return tx;return`${tx.slice(0,head)}...${tx.slice(-tail)}`}function walletLooksResolved(value){const wallet=normalizeWallet(value);if(!wallet)return false;if(wallet==="-")return false;const lower=wallet.toLowerCase();if(lower==="unknown"||lower==="unattributed"||lower==="(none)"||lower==="none"||lower==="null"||lower==="n/a"){return false}if(lower.startsWith("market:"))return false;return true}function resolveWalletIdentity(rawWallet,options={}){const wallet=normalizeWallet(rawWallet);const tracked=Boolean(options.tracked);const allowUnresolved=Boolean(options.allowUnresolved);if(walletLooksResolved(wallet)){return{wallet,label:shortWallet(wallet),title:wallet,resolved:true,kind:tracked?"tracked":"resolved"}}if(tracked){return{wallet:"",label:"Tracked Account",title:"Tracked account (address unavailable in payload)",resolved:false,kind:"tracked_fallback"}}if(allowUnresolved){return{wallet:"",label:"Unattributed Flow",title:"Wallet identity unavailable from source payload",resolved:false,kind:"unresolved"}}return{wallet:"",label:"-",title:"-",resolved:false,kind:"empty"}}function normalizePositionEventType(rawType,oldSize=NaN,newSize=NaN){const raw=String(rawType||"").trim().toLowerCase();const previousSize=n(oldSize,NaN);const nextSize=n(newSize,NaN);if(!raw){if(Number.isFinite(previousSize)&&Number.isFinite(nextSize)){if(previousSize<=1e-10&&nextSize>1e-10)return"position_opened";if(previousSize>1e-10&&nextSize<=1e-10)return"position_closed";if(nextSize<previousSize)return"position_closed_partial";if(nextSize>previousSize)return"position_increased"}return""}if(raw.includes("position_opened"))return"position_opened";if(raw.includes("position_closed_partial"))return"position_closed_partial";if(raw.includes("position_closed"))return"position_closed";if(raw.includes("position_size_changed")){if(Number.isFinite(previousSize)&&Number.isFinite(nextSize)){if(previousSize<=1e-10&&nextSize>1e-10)return"position_opened";if(nextSize<=1e-10)return"position_closed";if(nextSize<previousSize)return"position_closed_partial";if(nextSize>previousSize)return"position_increased"}return"position_size_changed"}return raw}function positionEventLabel(eventType){const normalized=String(eventType||"").trim().toLowerCase();if(normalized==="position_opened")return"Opened";if(normalized==="position_closed_partial")return"Partial Close";if(normalized==="position_closed")return"Closed";if(normalized==="position_increased")return"Increased";if(normalized==="position_size_changed")return"Size Changed";return titleCase(normalized||"event")}function positionEventTone(eventType){const normalized=String(eventType||"").trim().toLowerCase();if(normalized==="position_opened"||normalized==="position_increased")return"up";if(normalized==="position_closed_partial"||normalized==="position_closed")return"down";return"cool"}function positionEventSourceLabel(source){const normalized=String(source||"").trim().toLowerCase();if(!normalized)return"Unknown source";if(normalized.includes("ws_account_info"))return"WS account info";if(normalized.includes("ws_account_trade"))return"WS trade";if(normalized.includes("ws_account_order_update"))return"WS order";if(normalized.includes("wallet_first_positions"))return"REST reconcile";if(normalized.includes("wallet_first_position_change"))return"Live position change";if(normalized.includes("public_trade"))return"Public trade";return titleCase(normalized.replaceAll("_"," "))}function buildPositionEventRows(rows,trackedWallets,liveActiveByWallet){return safeArray(rows).map((row,idx)=>{const item=asObject(row);const rawSnapshot=asObject(item.raw);const rawType=item.eventType||item.sideEvent||item.cause||item.type;const eventType=normalizePositionEventType(rawType,item.oldSize,item.newSize);if(!eventType)return null;const detectedAt=tsMs(item.detectedAt||item.observedAt||item.updatedAt||item.timestamp||item.at||item.openedAt||0);const updatedAt=tsMs(item.updatedAt||item.observedAt||item.timestamp||detectedAt||0);const openedAt=tsMs(item.openedAt||detectedAt||updatedAt||0);const rawWallet=normalizeWallet(item.wallet||item.walletAddress||item.owner||item.trader||item.account||item.authority||"");const tracked=Boolean(item.trackedWallet)||(walletLooksResolved(rawWallet)?trackedWallets.has(rawWallet):false);const walletIdentity=resolveWalletIdentity(rawWallet,{tracked,allowUnresolved:true});const side=normalizeSide(item.side||item.rawSide||"");const oldSize=Number.isFinite(n(item.oldSize,NaN))?Math.abs(n(item.oldSize,0)):NaN;const newSize=Number.isFinite(n(item.newSize,NaN))?Math.abs(n(item.newSize,0)):Number.isFinite(n(item.amount,NaN))?Math.abs(n(item.amount,0)):NaN;let sizeDelta=n(item.sizeDelta,NaN);if(!Number.isFinite(sizeDelta)&&Number.isFinite(oldSize)&&Number.isFinite(newSize)){sizeDelta=newSize-oldSize}else if(!Number.isFinite(sizeDelta)&&eventType==="position_opened"&&Number.isFinite(newSize)){sizeDelta=newSize}else if(!Number.isFinite(sizeDelta)&&eventType==="position_closed"&&Number.isFinite(oldSize)){sizeDelta=-Math.abs(oldSize)}const sizeDeltaAbs=Number.isFinite(sizeDelta)?Math.abs(sizeDelta):Number.isFinite(n(item.sizeDeltaAbs,NaN))?Math.abs(n(item.sizeDeltaAbs,0)):NaN;const currentSize=Number.isFinite(newSize)?newSize:Number.isFinite(n(item.amount,NaN))?Math.abs(n(item.amount,0)):Number.isFinite(n(item.size,NaN))?Math.abs(n(item.size,0)):NaN;const entry=n(item.entry,n(item.price,NaN));const mark=n(item.mark,n(item.markPrice,NaN));const eventUsd=n(item.eventUsd,n(item.positionUsd,0));const margin=n(item.margin,n(item.marginUsd,n(item.margin_usd,n(rawSnapshot.margin,n(rawSnapshot.margin_usd,NaN)))));const leverageSeed=n(item.leverage,n(item.lev,n(item.leverageValue,n(item.leverage_value,n(rawSnapshot.leverage,n(rawSnapshot.lev,NaN))))));const leverage=Number.isFinite(leverageSeed)&&leverageSeed>0?leverageSeed:Number.isFinite(margin)&&margin>0&&Number.isFinite(eventUsd)?eventUsd/margin:NaN;const pnl=n(item.unrealizedPnlUsd,n(item.pnl,NaN));const freshness=walletIdentity.resolved?String(item.freshness||rowFreshnessByTs(detectedAt)).trim().toLowerCase()||"unknown":"unresolved";const status=walletIdentity.resolved?eventType||freshness:"unresolved";const ageMs=detectedAt?Math.max(0,Date.now()-detectedAt):NaN;const positionState=derivePositionState(status,freshness,ageMs);const walletConfidence=String(item.walletConfidence||(walletIdentity.resolved?"hard_payload":"unresolved")).trim().toLowerCase();const txSignature=String(item.txSignature||item.signature||item.txid||"").trim();const tradeRefRaw=item.tradeRef||item.positionKey||item.historyId||item.orderId||item.i||item.li||null;const tradeRef=tradeRefRaw!==null&&tradeRefRaw!==undefined?String(tradeRefRaw):"";const tradeRefType=String(item.tradeRefType||(item.positionKey?"wallet_position":item.historyId?"history_id":item.orderId||item.i?"order_id":item.li?"li":"unknown"));const txConfidence=String(item.txConfidence||(txSignature?"hard_payload":"unresolved")).trim().toLowerCase();const liveWallet=walletIdentity.resolved?liveActiveByWallet.get(walletIdentity.wallet):null;return{key:`p:evt:${item.positionKey||item.key||item.wallet||"na"}:${eventType||"event"}:${detectedAt||idx}`,timestamp:detectedAt||updatedAt||openedAt,detectedAt,updatedAt,openedAt,wallet:walletIdentity.wallet,walletLabel:walletIdentity.label,walletTitle:walletIdentity.title,walletResolved:walletIdentity.resolved,trackedWallet:tracked,symbol:String(item.symbol||"-").toUpperCase(),side,oldSize,newSize:Number.isFinite(newSize)?newSize:NaN,currentSize,sizeDelta,sizeDeltaAbs,size:currentSize,amount:currentSize,eventUsd,positionUsd:n(item.positionUsd,eventUsd),entry,mark,pnl,unrealizedPnlUsd:n(item.unrealizedPnlUsd,pnl),margin,funding:NaN,leverage,roe:NaN,liquidationPrice:NaN,status,freshness,positionState,eventType,eventLabel:positionEventLabel(eventType),eventTone:positionEventTone(eventType),source:String(item.source||"wallet_first_position_change"),sourceLabel:positionEventSourceLabel(item.source||"wallet_first_position_change"),walletSource:String(item.walletSource||"wallet_positions_api").trim().toLowerCase(),walletConfidence,txSignature,txSource:String(item.txSource||(txSignature?"payload":"wallet_positions_api")).trim().toLowerCase(),txConfidence,tradeRef,tradeRefType,ageMs,activityLevel:activityLevelFromAge(ageMs),walletLiveRank:(liveWallet===null||liveWallet===void 0?void 0:liveWallet.rank)||null,walletOpenPositions:(liveWallet===null||liveWallet===void 0?void 0:liveWallet.openPositions)||0,walletRecentEvents15m:(liveWallet===null||liveWallet===void 0?void 0:liveWallet.recentEvents15m)||0,walletRecentEvents1h:(liveWallet===null||liveWallet===void 0?void 0:liveWallet.recentEvents1h)||0,raw:item}}).filter(Boolean).sort((a,b)=>Number(b.detectedAt||b.updatedAt||b.openedAt||0)-Number(a.detectedAt||a.updatedAt||a.openedAt||0)||Number(b.updatedAt||0)-Number(a.updatedAt||0))}function rowFreshnessByTs(timestampMs){if(!timestampMs)return"unknown";const age=Date.now()-timestampMs;if(age<=5*60*1000)return"fresh";if(age<=30*60*1000)return"cooling";return"stale"}function clamp(value,min,max){const numeric=n(value,NaN);if(!Number.isFinite(numeric))return min;return Math.min(max,Math.max(min,numeric))}function titleCase(value){return String(value||"").trim().replaceAll("_"," ").replace(/\s+/g," ").replace(/\b\w/g,char=>char.toUpperCase())}async function copyTextToClipboard(text){var _navigator;const value=String(text||"").trim();if(!value)return false;if((_navigator=navigator)!==null&&_navigator!==void 0&&(_navigator=_navigator.clipboard)!==null&&_navigator!==void 0&&_navigator.writeText){await navigator.clipboard.writeText(value);return true}const input=document.createElement("textarea");input.value=value;input.setAttribute("readonly","readonly");input.style.position="fixed";input.style.opacity="0";document.body.appendChild(input);input.select();let copied=false;try{copied=document.execCommand("copy")}finally{document.body.removeChild(input)}return copied}function activityLevelFromAge(ageMs){const age=Math.max(0,n(ageMs,Infinity));if(age<=60*1000)return 5;if(age<=5*60*1000)return 4;if(age<=15*60*1000)return 3;if(age<=30*60*1000)return 2;if(age<=60*60*1000)return 1;return 0}function derivePositionState(status,freshness,ageMs){const normalizedStatus=String(status||"").trim().toLowerCase();if(normalizedStatus.includes("clos"))return{label:"closing",tone:"down"};if(normalizedStatus.includes("reduc"))return{label:"reduced",tone:"down"};if(normalizedStatus.includes("increas")||normalizedStatus.includes("add")){return{label:"increased",tone:"up"}}if(freshness==="stale")return{label:"stale",tone:"stale"};if(n(ageMs,Infinity)<=15*60*1000)return{label:"new",tone:"up"};if(freshness==="fresh")return{label:"live",tone:"fresh"};if(freshness==="cooling")return{label:"watch",tone:"cool"};return{label:normalizedStatus||freshness||"watch",tone:"cool"}}function topSymbolSharePct(symbolVolumes,totalVolumeUsd){const values=Object.values(asObject(symbolVolumes)).map(value=>n(value,NaN)).filter(value=>Number.isFinite(value)&&value>0);const total=n(totalVolumeUsd,NaN);if(!values.length||!Number.isFinite(total)||total<=0)return NaN;return Math.max(...values)/total*100}function computeWalletScores(metrics){const exposureFactor=Math.min(1,Math.max(0,n(metrics.exposureUsd,0))/250000);const avgTradeUsd=n(metrics.totalTrades,0)>0?n(metrics.totalVolumeUsd,0)/Math.max(1,n(metrics.totalTrades,0)):0;const avgTradeFactor=Math.min(1,avgTradeUsd/25000);const liveEventFactor=Math.min(1,n(metrics.recentEvents15m,0)/15);const positiveWindows=[metrics.pnl7Usd,metrics.pnl30Usd,metrics.totalPnlUsd].filter(value=>Number.isFinite(n(value,NaN))&&n(value,0)>0).length;const winRate=clamp(metrics.winRate,0,100);const concentrationPct=clamp(metrics.concentrationPct,0,100);const convictionScore=clamp(avgTradeFactor*35+exposureFactor*30+Math.min(n(metrics.openPositions,0),12)*2+liveEventFactor*11+concentrationPct*0.12,0,100);const consistencyScore=clamp(winRate*0.58+positiveWindows*12+(n(metrics.totalTrades,0)>20?12:n(metrics.totalTrades,0)>5?6:0),0,100);const riskScore=clamp(concentrationPct*0.55+exposureFactor*25+(metrics.freshness==="stale"?10:0)+(n(metrics.openPositions,0)<=1&&exposureFactor>0.2?12:0),0,100);const liveActiveScore=clamp(liveEventFactor*45+Math.min(n(metrics.openPositions,0),20)*1.5+exposureFactor*25+(metrics.freshness==="fresh"?20:metrics.freshness==="cooling"?12:metrics.freshness==="stale"?0:6),0,100);return{convictionScore,consistencyScore,concentrationScore:concentrationPct,riskScore,liveActiveScore}}function deriveWalletBehaviorTags(metrics){const tags=[];if(n(metrics.recentEvents15m,0)>=10)tags.push("live burst");if(n(metrics.openPositions,0)>=12)tags.push("broad book");if(n(metrics.concentrationPct,0)>=55)tags.push("concentrated");if(n(metrics.winRate,0)>=55&&n(metrics.totalPnlUsd,0)>0)tags.push("disciplined");if(n(metrics.pnl7Usd,0)>0&&n(metrics.pnl30Usd,0)>0)tags.push("trend intact");if(n(metrics.increaseCount,0)>=3&&n(metrics.increaseCount,0)>n(metrics.reduceCount,0)){tags.push("scales in")}if(n(metrics.reduceCount,0)>=3&&n(metrics.reduceCount,0)>=n(metrics.increaseCount,0)){tags.push("scales out")}if(n(metrics.avgHoldingMs,0)>=24*60*60*1000)tags.push("patient holds");if(n(metrics.avgHoldingMs,0)>0&&n(metrics.avgHoldingMs,0)<=2*60*60*1000&&n(metrics.totalTrades,0)>=15){tags.push("fast rotation")}if(metrics.freshness==="stale")tags.push("stale watch");if(n(metrics.totalPnlUsd,0)<0&&n(metrics.recentEvents1h,0)>0)tags.push("drawdown");if(!tags.length&&n(metrics.openPositions,0)===0)tags.push("quiet");return tags.slice(0,3)}function scoreTone(score,inverse=false){const numeric=n(score,NaN);if(!Number.isFinite(numeric))return"mid";if(inverse){if(numeric>=70)return"bad";if(numeric>=45)return"low";return"high"}if(numeric>=70)return"high";if(numeric>=45)return"mid";return"low"}function walletWinRateProfile(row){if(n(row.trades7,0)>=8&&Number.isFinite(n(row.winRate7,NaN))){return{valid:true,rate:n(row.winRate7,NaN),trades:n(row.trades7,0),windowLabel:"7D"}}if(n(row.trades30,0)>=15&&Number.isFinite(n(row.winRate30,NaN))){return{valid:true,rate:n(row.winRate30,NaN),trades:n(row.trades30,0),windowLabel:"30D"}}if(n(row.totalTrades,0)>=30&&Number.isFinite(n(row.winRate,NaN))){return{valid:true,rate:n(row.winRate,NaN),trades:n(row.totalTrades,0),windowLabel:"All"}}return{valid:false,rate:NaN,trades:n(row.totalTrades,0),windowLabel:""}}function walletPerformanceFocus(row){const pnl30=n(row.pnl30Usd,NaN);const pnl7=n(row.pnl7Usd,NaN);if(Number.isFinite(pnl30)&&Math.abs(pnl30)>0){return{value:pnl30,label:"30D PnL",text:fmtSignedUsd(pnl30,0)}}if(Number.isFinite(pnl7)&&Math.abs(pnl7)>0){return{value:pnl7,label:"7D PnL",text:fmtSignedUsd(pnl7,0)}}return{value:n(row.totalPnlUsd,0),label:"Total PnL",text:fmtSignedUsd(row.totalPnlUsd,0)}}function buildWalletRankingGroups(rows){const list=safeArray(rows).filter(row=>row&&row.walletResolved);const buildGroup=(key,title,subtitle,tone,rankedRows)=>({key,title,subtitle,tone,rankedRows,count:rankedRows.length});const mostActive=list.filter(row=>n(row.recentEvents15m,0)>0||n(row.recentEvents1h,0)>0||n(row.openPositions,0)>0).sort((a,b)=>n(b.recentEvents15m,0)-n(a.recentEvents15m,0)||n(b.recentEvents1h,0)-n(a.recentEvents1h,0)||n(b.liveActiveScore,0)-n(a.liveActiveScore,0)||tsMs(b.lastActivity)-tsMs(a.lastActivity));const highestVolume=list.filter(row=>n(row.volume7Usd,0)>0||n(row.volume30Usd,0)>0||n(row.totalVolumeUsd,0)>0||n(row.exposureUsd,0)>0).sort((a,b)=>Math.max(n(b.volume7Usd,0),n(b.volume30Usd,0),n(b.totalVolumeUsd,0))-Math.max(n(a.volume7Usd,0),n(a.volume30Usd,0),n(a.totalVolumeUsd,0))||n(b.exposureUsd,0)-n(a.exposureUsd,0));const highestWinRate=list.map(row=>({row,profile:walletWinRateProfile(row)})).filter(entry=>entry.profile.valid).sort((a,b)=>n(b.profile.rate,0)-n(a.profile.rate,0)||n(b.profile.trades,0)-n(a.profile.trades,0)||n(b.row.totalPnlUsd,0)-n(a.row.totalPnlUsd,0)).map(entry=>({...entry.row,winRateProfile:entry.profile}));const strongestPnl=list.filter(row=>Number.isFinite(n(row.pnl30Usd,NaN))||Number.isFinite(n(row.pnl7Usd,NaN))||Number.isFinite(n(row.totalPnlUsd,NaN))).sort((a,b)=>walletPerformanceFocus(b).value-walletPerformanceFocus(a).value||n(b.totalPnlUsd,0)-n(a.totalPnlUsd,0));const highestConviction=list.filter(row=>n(row.convictionScore,0)>0||n(row.exposureUsd,0)>0).sort((a,b)=>n(b.convictionScore,0)-n(a.convictionScore,0)||n(b.exposureUsd,0)-n(a.exposureUsd,0)||n(b.concentrationPct,0)-n(a.concentrationPct,0));const mostConsistent=list.filter(row=>n(row.totalTrades,0)>=20&&n(row.consistencyScore,0)>0).sort((a,b)=>n(b.consistencyScore,0)-n(a.consistencyScore,0)||n(b.totalTrades,0)-n(a.totalTrades,0)||n(b.totalPnlUsd,0)-n(a.totalPnlUsd,0));return[buildGroup("active","Most Active Wallets","Highest recent live activity and position motion.","cool",mostActive),buildGroup("volume","Highest Volume Wallets","Largest recent traded volume and exposure.","cool",highestVolume),buildGroup("winrate","Highest Win Rate Wallets","Best hit rate where sample size is meaningful.","up",highestWinRate),buildGroup("pnl","Strongest PnL Wallets","Best realized + live performance windows.","up",strongestPnl),buildGroup("conviction","Highest Conviction Wallets","Concentrated exposure with stronger persistence.","cool",highestConviction),buildGroup("consistency","Most Consistent Wallets","Repeatable quality over a meaningful sample.","up",mostConsistent)].filter(group=>group.count>0)}function walletGroupMetric(groupKey,row){if(groupKey==="active"){return{primary:`${fmtInt(row.recentEvents15m||0)} ev / 15m`,secondary:`${fmtInt(row.openPositions||0)} open • ${fmtAgo(row.lastActivity)}`}}if(groupKey==="volume"){const primaryValue=n(row.volume7Usd,0)>0?row.volume7Usd:n(row.volume30Usd,0)>0?row.volume30Usd:row.totalVolumeUsd;const primaryLabel=n(row.volume7Usd,0)>0?"7D":n(row.volume30Usd,0)>0?"30D":"All";return{primary:`${primaryLabel} ${fmtUsd(primaryValue,0)}`,secondary:`${fmtUsd(row.exposureUsd,0)} live exposure`}}if(groupKey==="winrate"){const profile=row.winRateProfile||walletWinRateProfile(row);return{primary:`${fmtPct(profile.rate,1)} ${profile.windowLabel}`,secondary:`${fmtInt(profile.trades)} trades • ${fmtSignedUsd(row.totalPnlUsd,0)}`}}if(groupKey==="pnl"){const focus=walletPerformanceFocus(row);return{primary:`${focus.label} ${focus.text}`,secondary:`Total ${fmtSignedUsd(row.totalPnlUsd,0)}`}}if(groupKey==="conviction"){return{primary:`Conviction ${fmtInt(row.convictionScore||0)}`,secondary:`${fmtUsd(row.exposureUsd,0)} • ${fmtInt(row.concentrationPct||0)}% concentration`}}return{primary:`Consistency ${fmtInt(row.consistencyScore||0)}`,secondary:`${fmtPct(row.winRate,1)} • ${fmtInt(row.totalTrades||0)} trades`}}function getUniqueValues(rows,key){return Array.from(new Set(rows.map(row=>String((row===null||row===void 0?void 0:row[key])||"").trim()).filter(Boolean).sort((a,b)=>a.localeCompare(b))))}function buildRows(payload,options={}){var _payload$summary0,_payload$environment,_state$rows;const includePositions=options.includePositions!==false;const includeWallets=options.includeWallets!==false;const includeLiveActive=options.includeLiveActive!==false;const positionFeedMode=String((payload===null||payload===void 0?void 0:payload.positionFeedMode)||(payload===null||payload===void 0||(_payload$summary0=payload.summary)===null||_payload$summary0===void 0?void 0:_payload$summary0.positionFeedMode)||"").trim().toLowerCase();const account=normalizeWallet((payload===null||payload===void 0||(_payload$environment=payload.environment)===null||_payload$environment===void 0?void 0:_payload$environment.account)||"");const publicTrades=extractPublicTrades(payload);const positionsPrimary=includePositions?extractPositions(payload):[];const positionsFallback=includePositions?extractPositions(payload):[];const walletPerformance=includeWallets?extractWalletPerformance(payload):[];const liveActiveSourceRows=includePositions||includeWallets||includeLiveActive?extractLiveActiveWallets(payload).map((row,idx)=>{const item=asObject(row);const walletIdentity=resolveWalletIdentity(item.wallet||"",{tracked:true,allowUnresolved:true});return{key:`la:${item.wallet||"na"}:${idx}`,rank:idx+1,wallet:walletIdentity.wallet,walletLabel:walletIdentity.label,walletTitle:walletIdentity.title,walletResolved:walletIdentity.resolved,trackedWallet:true,freshness:String(item.freshness||"unknown").trim().toLowerCase(),openPositions:n(item.openPositions,0),positionUsd:n(item.positionUsd,0),recentEvents15m:n(item.recentEvents15m,0),recentEvents1h:n(item.recentEvents1h,0),lastActivityAt:tsMs(item.lastActivityAt||0),lastPositionAt:tsMs(item.lastPositionAt||0),liveScannedAt:tsMs(item.liveScannedAt||0),symbols:safeArray(item.symbols).map(value=>String(value||"").trim()).filter(Boolean),d24:asObject(item.d24),d30:asObject(item.d30),all:asObject(item.all),activityScore:n(item.activityScore,NaN),raw:item}}):[];const liveActiveByWallet=new Map(liveActiveSourceRows.filter(row=>walletLooksResolved(row.wallet)).map(row=>[normalizeWallet(row.wallet),row]));const trackedWallets=includeWallets?new Set(walletPerformance.map(row=>normalizeWallet((row===null||row===void 0?void 0:row.wallet)||(row===null||row===void 0?void 0:row.walletAddress))).filter(wallet=>walletLooksResolved(wallet))):new Set(safeArray((_state$rows=state.rows)===null||_state$rows===void 0?void 0:_state$rows.wallets).map(row=>normalizeWallet(row===null||row===void 0?void 0:row.wallet)).filter(wallet=>walletLooksResolved(wallet)));const positionRows=includePositions?(()=>{const primaryContainsPositionEvents=positionsPrimary.some(row=>Boolean(normalizePositionEventType(row&&(row.eventType||row.sideEvent||row.cause||row.type),row&&row.oldSize,row&&row.newSize)));if(positionFeedMode!=="current"&&primaryContainsPositionEvents){const positionEventRows=buildPositionEventRows(positionsPrimary,trackedWallets,liveActiveByWallet);if(positionEventRows.length>0)return positionEventRows}const positionRowsFromPrimary=positionsPrimary.map((row,idx)=>{const item=asObject(row);const rawSnapshot=asObject(item.raw);const timestamp=tsMs(item.timestamp||item.updatedAt||item.lastTradeAt||item.lastUpdatedAt||item.openedAt||0);const rawWallet=normalizeWallet(item.wallet||item.walletAddress||item.owner||item.trader||item.account||item.authority||item.user||"");const tracked=Boolean(item.trackedWallet)||(walletLooksResolved(rawWallet)?trackedWallets.has(rawWallet):false);const walletIdentity=resolveWalletIdentity(rawWallet,{tracked,allowUnresolved:true});const side=normalizeSide(item.side||item.rawSide||"");const size=Math.abs(n(item.size,n(item.amount,n(item.qty,0))));const entry=n(item.entry,n(item.entryPrice,n(item.entry_price,n(item.price,NaN))));const mark=n(item.mark,n(item.markPrice,n(item.mark_price,n(item.price,entry))));const positionUsd=n(item.positionUsd,n(item.notionalUsd,Number.isFinite(mark)?Math.abs(size*mark):Number.isFinite(entry)?Math.abs(size*entry):0));const margin=n(item.margin,n(item.marginUsd,n(item.margin_usd,n(rawSnapshot.margin,n(rawSnapshot.margin_usd,NaN)))));const funding=n(item.funding,n(item.fundingUsd,n(rawSnapshot.funding,NaN)));const pnl=n(item.pnl,n(item.unrealizedPnlUsd,n(item.unrealizedPnl,NaN)));const liquidationPrice=n(item.liquidationPrice,n(item.liquidation_price,n(rawSnapshot.liquidation_price,NaN)));const lifecycle=asObject(item.lifecycle);const openedAt=tsMs(item.openedAt||item.createdAt||item.openTime||rawSnapshot.created_at||timestamp);const lifecycleAgeMs=n(lifecycle.currentAgeMs,NaN);const ageMs=openedAt?Math.max(0,Date.now()-openedAt):Number.isFinite(lifecycleAgeMs)?lifecycleAgeMs:NaN;const baseFreshness=String(item.freshness||"").trim().toLowerCase();const freshness=walletIdentity.resolved?baseFreshness||rowFreshnessByTs(timestamp):"unresolved";const statusRaw=String(item.status||lifecycle.lastChangeType||"").trim().toLowerCase();const status=walletIdentity.resolved?statusRaw||freshness:"unresolved";const positionState=derivePositionState(status,freshness,ageMs);const leverageSeed=n(item.leverage,n(item.lev,n(item.leverageValue,n(item.leverage_value,n(rawSnapshot.leverage,n(rawSnapshot.lev,NaN))))));const leverage=Number.isFinite(leverageSeed)&&leverageSeed>0?leverageSeed:Number.isFinite(margin)&&margin>0&&Number.isFinite(positionUsd)?positionUsd/margin:NaN;const roe=Number.isFinite(margin)&&margin>0&&Number.isFinite(pnl)?pnl/margin*100:NaN;const walletConfidence=String(item.walletConfidence||(walletIdentity.resolved?"hard_payload":"unresolved")).trim().toLowerCase();const txSignature=String(item.txSignature||item.signature||item.txid||"").trim();const tradeRefRaw=item.tradeRef||item.positionKey||item.historyId||item.orderId||item.i||item.li||null;const tradeRef=tradeRefRaw!==null&&tradeRefRaw!==undefined?String(tradeRefRaw):"";const tradeRefType=String(item.tradeRefType||(item.positionKey?"wallet_position":item.historyId?"history_id":item.orderId||item.i?"order_id":item.li?"li":"unknown"));const txConfidence=String(item.txConfidence||(txSignature?"hard_payload":"unresolved")).trim().toLowerCase();const liveWallet=walletIdentity.resolved?liveActiveByWallet.get(walletIdentity.wallet):null;return{key:`p:pos:${item.positionKey||item.id||item.historyId||`${item.symbol||"na"}:${item.side||"na"}:${idx}`}`,timestamp,wallet:walletIdentity.wallet,walletLabel:walletIdentity.label,walletTitle:walletIdentity.title,walletResolved:walletIdentity.resolved,trackedWallet:tracked,symbol:String(item.symbol||"-").toUpperCase(),side,size,positionUsd,entry,mark,pnl,margin,funding,leverage,roe,liquidationPrice,status,freshness,positionState,walletSource:String(item.walletSource||"").trim().toLowerCase()||"wallet_positions_api",walletConfidence,txSignature,txSource:String(item.txSource||(txSignature?"payload":"wallet_positions_api")).trim().toLowerCase(),txConfidence,tradeRef,tradeRefType,openedAt,updatedAt:timestamp,ageMs,activityLevel:activityLevelFromAge(Date.now()-timestamp),walletLiveRank:(liveWallet===null||liveWallet===void 0?void 0:liveWallet.rank)||null,walletOpenPositions:(liveWallet===null||liveWallet===void 0?void 0:liveWallet.openPositions)||0,walletRecentEvents15m:(liveWallet===null||liveWallet===void 0?void 0:liveWallet.recentEvents15m)||0,walletRecentEvents1h:(liveWallet===null||liveWallet===void 0?void 0:liveWallet.recentEvents1h)||0,lifecycle,source:String(item.source||"wallet_first_positions"),raw:item}}).sort((a,b)=>Number(b.openedAt||0)-Number(a.openedAt||0)||Number(b.updatedAt||0)-Number(a.updatedAt||0));if(positionRowsFromPrimary.length>0)return positionRowsFromPrimary;const positionRowsFromPublic=publicTrades.map((row,idx)=>{const item=asObject(row);const timestamp=tsMs(item.timestamp||item.updatedAt||item.lastTradeAt||item.lastUpdatedAt||item.openedAt||0);const rawWallet=normalizeWallet(item.wallet||item.walletAddress||item.owner||item.trader||item.account||item.authority||item.user||"");const tracked=walletLooksResolved(rawWallet)?trackedWallets.has(rawWallet):false;const walletIdentity=resolveWalletIdentity(rawWallet,{tracked,allowUnresolved:true});const side=normalizeSide(item.side||"");const size=Math.abs(n(item.amount,n(item.size,n(item.qty,0))));const entry=n(item.entry,n(item.price,n(item.entryPrice,NaN)));const mark=n(item.mark,n(item.price,n(item.currentPrice,NaN)));const positionUsd=n(item.notionalUsd,n(item.openNotional,Number.isFinite(entry)?size*entry:0));const pnl=n(item.pnl,n(item.unrealizedPnlUsd,n(item.unrealizedPnl,NaN)));const openedAt=timestamp;const ageMs=openedAt?Math.max(0,Date.now()-openedAt):NaN;const freshness=walletIdentity.resolved?rowFreshnessByTs(timestamp):"unresolved";const cause=String(item.cause||"").trim().toLowerCase();const status=walletIdentity.resolved?cause||freshness:"unresolved";const positionState=derivePositionState(status,freshness,ageMs);const walletConfidence=String(item.walletConfidence||(walletIdentity.resolved?"hard_payload":"unresolved")).trim().toLowerCase();const txSignature=String(item.txSignature||item.signature||item.txid||"").trim();const tradeRefRaw=item.tradeRef||item.historyId||item.orderId||item.i||item.li||null;const tradeRef=tradeRefRaw!==null&&tradeRefRaw!==undefined?String(tradeRefRaw):"";const tradeRefType=String(item.tradeRefType||(item.historyId?"history_id":item.orderId||item.i?"order_id":item.li?"li":"unknown"));const txConfidence=String(item.txConfidence||(txSignature?"hard_payload":"unresolved")).trim().toLowerCase();const liveWallet=walletIdentity.resolved?liveActiveByWallet.get(walletIdentity.wallet):null;return{key:`p:pub:${item.historyId||item.id||item.accountId||idx}`,timestamp,wallet:walletIdentity.wallet,walletLabel:walletIdentity.label,walletTitle:walletIdentity.title,walletResolved:walletIdentity.resolved,trackedWallet:tracked,symbol:String(item.symbol||"-").toUpperCase(),side,size,positionUsd,entry,mark,pnl,margin:NaN,funding:NaN,leverage:NaN,roe:NaN,liquidationPrice:NaN,status,freshness,positionState,walletSource:String(item.walletSource||"").trim().toLowerCase(),walletConfidence,txSignature,txSource:String(item.txSource||(txSignature?"payload":"unresolved")).trim().toLowerCase(),txConfidence,tradeRef,tradeRefType,openedAt,updatedAt:timestamp,ageMs,activityLevel:activityLevelFromAge(Date.now()-timestamp),walletLiveRank:(liveWallet===null||liveWallet===void 0?void 0:liveWallet.rank)||null,walletOpenPositions:(liveWallet===null||liveWallet===void 0?void 0:liveWallet.openPositions)||0,walletRecentEvents15m:(liveWallet===null||liveWallet===void 0?void 0:liveWallet.recentEvents15m)||0,walletRecentEvents1h:(liveWallet===null||liveWallet===void 0?void 0:liveWallet.recentEvents1h)||0,source:"exchange_public_trade",raw:item}}).sort((a,b)=>Number(b.openedAt||0)-Number(a.openedAt||0)||Number(b.updatedAt||0)-Number(a.updatedAt||0));const positionRowsFromFallback=positionsFallback.map((row,idx)=>{const item=asObject(row);const timestamp=tsMs(item.updatedAt||item.timestamp||item.openedAt||0);const rawWallet=normalizeWallet(item.wallet||item.account||account||"");const tracked=walletLooksResolved(rawWallet)?trackedWallets.has(rawWallet):true;const walletIdentity=resolveWalletIdentity(rawWallet,{tracked,allowUnresolved:true});const freshness=walletIdentity.resolved?rowFreshnessByTs(timestamp):"unresolved";const walletConfidence=walletIdentity.resolved?"hard_payload":"unresolved";const openedAt=tsMs(item.openedAt||item.openTime||0);const ageMs=openedAt?Math.max(0,Date.now()-openedAt):NaN;const pnl=n(item.unrealizedPnlUsd,NaN);const margin=n(item.margin,n(item.marginUsd,NaN));const positionUsd=n(item.notionalUsd,0);const leverage=Number.isFinite(margin)&&margin>0&&Number.isFinite(positionUsd)?positionUsd/margin:NaN;const roe=Number.isFinite(margin)&&margin>0&&Number.isFinite(pnl)?pnl/margin*100:NaN;const positionState=derivePositionState(item.riskTag||freshness,freshness,ageMs);const liveWallet=walletIdentity.resolved?liveActiveByWallet.get(walletIdentity.wallet):null;return{key:`p:fb:${item.symbol||"na"}:${item.side||"na"}:${idx}`,timestamp,wallet:walletIdentity.wallet,walletLabel:walletIdentity.label,walletTitle:walletIdentity.title,walletResolved:walletIdentity.resolved,trackedWallet:tracked,symbol:String(item.symbol||"-").toUpperCase(),side:normalizeSide(item.side||""),size:n(item.amountAbs,n(item.size,0)),positionUsd,entry:n(item.entryPrice,NaN),mark:n(item.markPrice,NaN),pnl,margin,funding:n(item.funding,NaN),leverage,roe,liquidationPrice:n(item.liquidationPrice,NaN),status:walletIdentity.resolved?item.riskTag||freshness:"unresolved",freshness,positionState,walletSource:"account_positions_fallback",walletConfidence,txSignature:"",txSource:"unresolved",txConfidence:"unresolved",tradeRef:"",tradeRefType:"unknown",openedAt,updatedAt:timestamp,ageMs,activityLevel:activityLevelFromAge(Date.now()-timestamp),walletLiveRank:(liveWallet===null||liveWallet===void 0?void 0:liveWallet.rank)||null,walletOpenPositions:(liveWallet===null||liveWallet===void 0?void 0:liveWallet.openPositions)||0,walletRecentEvents15m:(liveWallet===null||liveWallet===void 0?void 0:liveWallet.recentEvents15m)||0,walletRecentEvents1h:(liveWallet===null||liveWallet===void 0?void 0:liveWallet.recentEvents1h)||0,source:"account_positions_fallback",raw:item}}).sort((a,b)=>Number(b.openedAt||0)-Number(a.openedAt||0)||Number(b.updatedAt||0)-Number(a.updatedAt||0));return positionRowsFromPublic.length>0?positionRowsFromPublic:positionRowsFromFallback})():null;const walletRows=includeWallets?walletPerformance.map((row,idx)=>{var _ref2,_ref3,_ref4,_ref5,_all$realizedPnlUsd,_ref6,_ref7,_all$unrealizedPnlUsd,_ref8,_all$volumeUsd,_all$trades,_ref9,_all$feesPaidUsd,_ref0,_ref1,_all$winRatePct,_d24$pnlUsd,_d7$pnlUsd,_d30$pnlUsd,_ref10,_lifecycle$avgObserve;const item=asObject(row);const all=asObject(item.all||item.d24||item.d30||{});const d24=asObject(item.d24);const d7=asObject(item.d7);const d30=asObject(item.d30);const walletIdentity=resolveWalletIdentity(item.wallet||item.walletAddress||"",{tracked:true,allowUnresolved:true});const liveWallet=walletIdentity.resolved?liveActiveByWallet.get(walletIdentity.wallet):null;const historicalLastActivity=tsMs(all.lastTrade||all.last_trade||item.lastTradeAt||item.lastUpdatedAt||item.updatedAt||item.timestamp||0);const liveLastActivity=tsMs((liveWallet===null||liveWallet===void 0?void 0:liveWallet.lastActivityAt)||(liveWallet===null||liveWallet===void 0?void 0:liveWallet.lastPositionAt)||(liveWallet===null||liveWallet===void 0?void 0:liveWallet.liveScannedAt)||0);const lastActivity=Math.max(historicalLastActivity,liveLastActivity);const freshness=walletIdentity.resolved?rowFreshnessByTs(lastActivity):"unresolved";const realizedPnlUsd=n((_ref2=(_ref3=(_ref4=(_ref5=(_all$realizedPnlUsd=all.realizedPnlUsd)!==null&&_all$realizedPnlUsd!==void 0?_all$realizedPnlUsd:all.realized_pnl_usd)!==null&&_ref5!==void 0?_ref5:all.pnlUsd)!==null&&_ref4!==void 0?_ref4:item.realizedPnlUsd)!==null&&_ref3!==void 0?_ref3:item.pnlUsd)!==null&&_ref2!==void 0?_ref2:item.pnlAllTime,NaN);const unrealizedPnlUsd=n((_ref6=(_ref7=(_all$unrealizedPnlUsd=all.unrealizedPnlUsd)!==null&&_all$unrealizedPnlUsd!==void 0?_all$unrealizedPnlUsd:all.unrealized_pnl_usd)!==null&&_ref7!==void 0?_ref7:item.unrealizedPnlUsd)!==null&&_ref6!==void 0?_ref6:item.unrealizedPnl,NaN);const totalVolumeUsd=n((_ref8=(_all$volumeUsd=all.volumeUsd)!==null&&_all$volumeUsd!==void 0?_all$volumeUsd:item.volumeUsd)!==null&&_ref8!==void 0?_ref8:item.totalVolume,0);const totalTrades=n((_all$trades=all.trades)!==null&&_all$trades!==void 0?_all$trades:item.totalTrades,0);const feesPaidUsd=n((_ref9=(_all$feesPaidUsd=all.feesPaidUsd)!==null&&_all$feesPaidUsd!==void 0?_all$feesPaidUsd:all.fees_paid_usd)!==null&&_ref9!==void 0?_ref9:item.feesPaidUsd,NaN);const rawWinRate=n((_ref0=(_ref1=(_all$winRatePct=all.winRatePct)!==null&&_all$winRatePct!==void 0?_all$winRatePct:item.winRatePct)!==null&&_ref1!==void 0?_ref1:item.winRate)!==null&&_ref0!==void 0?_ref0:item.winRate30d,NaN);const winRate=Number.isFinite(rawWinRate)&&rawWinRate>=0&&rawWinRate<=1?rawWinRate*100:rawWinRate;const pnl24Usd=n((_d24$pnlUsd=d24.pnlUsd)!==null&&_d24$pnlUsd!==void 0?_d24$pnlUsd:d24.realizedPnlUsd,NaN);const pnl7Usd=n((_d7$pnlUsd=d7.pnlUsd)!==null&&_d7$pnlUsd!==void 0?_d7$pnlUsd:d7.realizedPnlUsd,NaN);const pnl30Usd=n((_d30$pnlUsd=d30.pnlUsd)!==null&&_d30$pnlUsd!==void 0?_d30$pnlUsd:d30.realizedPnlUsd,NaN);const topSymbolSharePctValue=topSymbolSharePct(all.symbolVolumes,totalVolumeUsd);const openPositions=n(liveWallet===null||liveWallet===void 0?void 0:liveWallet.openPositions,0);const exposureUsd=n(liveWallet===null||liveWallet===void 0?void 0:liveWallet.positionUsd,0);const recentEvents15m=n(liveWallet===null||liveWallet===void 0?void 0:liveWallet.recentEvents15m,0);const recentEvents1h=n(liveWallet===null||liveWallet===void 0?void 0:liveWallet.recentEvents1h,0);const lifecycle=asObject(item.lifecycle);const avgHoldingMs=n((_ref10=(_lifecycle$avgObserve=lifecycle.avgObservedHoldingMs)!==null&&_lifecycle$avgObserve!==void 0?_lifecycle$avgObserve:lifecycle.avgCompletedHoldingMs)!==null&&_ref10!==void 0?_ref10:lifecycle.currentAvgOpenAgeMs,NaN);const increaseCount=n(lifecycle.increaseCount,0);const reduceCount=n(lifecycle.reduceCount,0);const closeCount=n(lifecycle.closeCount,0);const completedPositionCount=n(lifecycle.completedPositionCount,0);const derivedScores=computeWalletScores({exposureUsd,totalVolumeUsd,totalTrades,recentEvents15m,openPositions,concentrationPct:topSymbolSharePctValue,winRate,pnl7Usd,pnl24Usd,pnl30Usd,totalPnlUsd:(walletIdentity.resolved&&Number.isFinite(realizedPnlUsd)?realizedPnlUsd:0)+(walletIdentity.resolved&&Number.isFinite(unrealizedPnlUsd)?unrealizedPnlUsd:0),freshness});const totalPnlUsd=(walletIdentity.resolved&&Number.isFinite(realizedPnlUsd)?realizedPnlUsd:0)+(walletIdentity.resolved&&Number.isFinite(unrealizedPnlUsd)?unrealizedPnlUsd:0);const behaviorTags=deriveWalletBehaviorTags({recentEvents15m,recentEvents1h,openPositions,concentrationPct:topSymbolSharePctValue,winRate,pnl7Usd,totalPnlUsd,freshness,increaseCount,reduceCount,avgHoldingMs,totalTrades});return{key:`w:${item.wallet||"na"}:${idx}`,wallet:walletIdentity.wallet||normalizeWallet(item.walletAddress||""),walletLabel:walletIdentity.label,walletTitle:walletIdentity.title,walletResolved:walletIdentity.resolved,trackedWallet:true,totalTrades:walletIdentity.resolved?totalTrades:NaN,totalVolumeUsd:walletIdentity.resolved?totalVolumeUsd:NaN,realizedPnlUsd:walletIdentity.resolved?realizedPnlUsd:NaN,unrealizedPnlUsd:walletIdentity.resolved?unrealizedPnlUsd:NaN,totalPnlUsd,winRate:walletIdentity.resolved?winRate:NaN,feesPaidUsd:walletIdentity.resolved?feesPaidUsd:NaN,lastActivity,freshness,status:freshness,pnl24Usd,pnl7Usd,pnl30Usd,trades24:n(d24.trades,NaN),trades7:n(d7.trades,NaN),trades30:n(d30.trades,NaN),volume24Usd:n(d24.volumeUsd,NaN),volume7Usd:n(d7.volumeUsd,NaN),volume30Usd:n(d30.volumeUsd,NaN),winRate7:n(d7.winRatePct,NaN),winRate30:n(d30.winRatePct,NaN),liveActiveRank:(liveWallet===null||liveWallet===void 0?void 0:liveWallet.rank)||null,openPositions,exposureUsd,recentEvents15m,recentEvents1h,activityScore:n(liveWallet===null||liveWallet===void 0?void 0:liveWallet.activityScore,NaN),concentrationPct:topSymbolSharePctValue,avgHoldingMs,increaseCount,reduceCount,closeCount,completedPositionCount,topSymbols:Object.entries(asObject(all.symbolVolumes)).map(([symbol,value])=>[String(symbol||"").trim(),n(value,NaN)]).filter(entry=>entry[0]&&Number.isFinite(entry[1])).sort((a,b)=>b[1]-a[1]).slice(0,5),behaviorTags,lifecycle,...derivedScores,raw:item}}).sort((a,b)=>n(b.liveActiveScore,0)-n(a.liveActiveScore,0)||n(b.totalPnlUsd,0)-n(a.totalPnlUsd,0)):null;const liveActiveRows=includeLiveActive?liveActiveSourceRows:null;return{positions:positionRows,wallets:walletRows,liveActive:liveActiveRows}}function refreshFilterOptions(rows){const freshnessSelect=el("lt-filter-freshness");if(!freshnessSelect)return;const currentFreshness=freshnessSelect.value;const freshnessStates=Array.from(new Set([...getUniqueValues(rows.positions,"freshness"),...getUniqueValues(rows.wallets,"freshness")])).sort((a,b)=>a.localeCompare(b));freshnessSelect.innerHTML=["<option value=\"\">All</option>"].concat(freshnessStates.map(value=>`<option value=\"${escapeHtml(value)}\">${escapeHtml(value)}</option>`)).join("");if(freshnessStates.includes(currentFreshness))freshnessSelect.value=currentFreshness}function matchesSearch(row,query){if(!query)return true;const haystack=[row.wallet,row.walletLabel,row.walletTitle,row.symbol,row.side,row.status,row.eventType,row.eventLabel,row.source,row.sourceLabel,row.totalTrades,row.totalVolumeUsd,row.realizedPnlUsd,row.unrealizedPnlUsd,row.positionUsd,row.eventUsd,row.tradeRef,row.tradeRefType,row.txSignature,row.txSource].join(" ").toLowerCase();return haystack.includes(query)}function withinRange(value,minValue,maxValue){const min=n(minValue,0);const max=n(maxValue,0);const numeric=n(value,NaN);if(!Number.isFinite(numeric))return min===0&&max===0;if(min!==0&&numeric<min)return false;if(max!==0&&numeric>max)return false;return true}function resetPagination(){state.pagination.positions.page=1;state.pagination.wallets.page=1}function currentWindowParams(){const positions=state.pagination.positions||{page:1,pageSize:20};const wallets=state.pagination.wallets||{page:1,pageSize:100};const positionsPage=Math.max(1,Math.floor(n(positions.page,1)));const walletsPage=Math.max(1,Math.floor(n(wallets.page,1)));const positionsPageSize=Math.max(20,Math.min(20,Math.floor(n(positions.pageSize,20))));const walletsPageSize=Math.max(10,Math.min(5000,Math.floor(n(wallets.pageSize,100))));return{public_offset:(positionsPage-1)*positionsPageSize,public_limit:positionsPageSize,position_offset:(positionsPage-1)*positionsPageSize,position_limit:positionsPageSize,position_sort:state.positionsSortKey,position_dir:state.positionsSortDir,wallet_offset:(walletsPage-1)*walletsPageSize,wallet_limit:walletsPageSize,wallet_sort:state.walletsSortKey,wallet_dir:state.walletsSortDir}}function buildLiveTradesPath(basePath,options={}){var _state$filters;const params=new URLSearchParams;const window=currentWindowParams();for(const[key,value]of Object.entries(window)){params.set(key,String(value))}const walletFilter=String(((_state$filters=state.filters)===null||_state$filters===void 0?void 0:_state$filters.wallet)||"").trim();const searchFilter=String(state.search||"").trim();if(walletFilter)params.set("wallet",walletFilter);if(searchFilter)params.set("q",searchFilter);params.set("skip_public","1");params.set("live_active_offset","0");params.set("live_active_limit","12");if(options&&options.cacheBust!==undefined&&options.cacheBust!==null){params.set("_rt",String(options.cacheBust))}return`${basePath}?${params.toString()}`}function buildLiveTradesPositionsPath(feedMode="current",options={}){var _state$filters2;const params=new URLSearchParams;const requestedFeedMode=String(feedMode||"current").trim().toLowerCase()||"current";params.set("feed_mode",requestedFeedMode);params.set("all",options.all===false?"0":"1");params.set("position_sort",String(state.positionsSortKey||"openedAt").trim());params.set("position_dir",String(state.positionsSortDir||"desc").trim());params.set("position_limit",String(Math.max(100,n(options.positionLimit,5000))));if(requestedFeedMode!=="current"){const detectedSince=Number.isFinite(n(options.detectedSince,NaN))&&n(options.detectedSince,0)>0?Math.max(0,Math.floor(n(options.detectedSince,0))):0;if(detectedSince>0)params.set("detected_since",String(detectedSince))}const walletFilter=String(((_state$filters2=state.filters)===null||_state$filters2===void 0?void 0:_state$filters2.wallet)||"").trim();const searchFilter=String(state.search||"").trim();if(walletFilter)params.set("wallet",walletFilter);if(searchFilter)params.set("q",searchFilter);params.set("_rt",String(Date.now()));return`/api/live-trades/wallet-first/positions?${params.toString()}`}function mergeLiveTradePayloads(basePayload,positionsPayload=null,eventSummaryPayload=null){const nextPayload={...(asObject(basePayload)||{})};const nextSummary={...asObject(nextPayload.summary)};if(positionsPayload&&typeof positionsPayload==="object"){var _positionsPayload$sta;const positionRows=extractRows(positionsPayload.positions);const positionsSummary=asObject(positionsPayload.summary);const feedMode=String(positionsPayload.feedMode||((_positionsPayload$sta=positionsPayload.status)===null||_positionsPayload$sta===void 0?void 0:_positionsPayload$sta.feedMode)||positionsSummary.feedMode||positionsSummary.positionFeedMode||"current").trim().toLowerCase()||"current";nextPayload.positions=positionRows;nextPayload.positionFeedMode=feedMode;nextSummary.positionEventsTotal=n(positionsSummary.positionEventsTotal,n(positionsPayload.positionEventsTotal,n(nextSummary.positionEventsTotal,positionRows.length)));nextSummary.positionEventOpened1m=n(positionsSummary.positionEventOpened1m,n(positionsPayload.positionEventOpened1m,n(nextSummary.positionEventOpened1m,0)));nextSummary.positionEventPartialClosed1m=n(positionsSummary.positionEventPartialClosed1m,n(positionsPayload.positionEventPartialClosed1m,n(nextSummary.positionEventPartialClosed1m,0)));nextSummary.positionEventClosed1m=n(positionsSummary.positionEventClosed1m,n(positionsPayload.positionEventClosed1m,n(nextSummary.positionEventClosed1m,0)));nextSummary.positionEventDetectionP95Ms1m=n(positionsSummary.positionEventDetectionP95Ms1m,n(positionsPayload.positionEventDetectionP95Ms1m,n(nextSummary.positionEventDetectionP95Ms1m,NaN)));nextSummary.positionEventFeedPath=String(positionsSummary.positionEventFeedPath||positionsPayload.positionEventFeedPath||"/api/live-trades/wallet-first/positions?feed_mode=events").trim()||"/api/live-trades/wallet-first/positions?feed_mode=events";nextSummary.openPositionsTotal=n(positionsSummary.openPositionsTotal,n(positionsPayload.openPositionsTotal,positionRows.length));nextSummary.openPositions=n(positionsSummary.openPositions,positionRows.length);nextSummary.openPositionsWindowed=Boolean(positionsSummary.openPositionsWindowed);nextSummary.openPositionsHasMore=Boolean(positionsSummary.openPositionsHasMore);nextSummary.openPositionsOffset=n(positionsSummary.openPositionsOffset,0);nextSummary.openPositionsLimit=n(positionsSummary.openPositionsLimit,positionRows.length);nextSummary.openPositionsSort=String(positionsSummary.openPositionsSort||state.positionsSortKey||"openedAt").trim();nextSummary.openPositionsDir=String(positionsSummary.openPositionsDir||state.positionsSortDir||"desc").trim();nextSummary.positionFeedMode=feedMode}if(eventSummaryPayload&&typeof eventSummaryPayload==="object"){const eventSummary=asObject(eventSummaryPayload.summary);nextSummary.positionEventsTotal=n(eventSummary.positionEventsTotal,n(nextSummary.positionEventsTotal,0));nextSummary.positionEventOpened1m=n(eventSummary.positionEventOpened1m,n(nextSummary.positionEventOpened1m,0));nextSummary.positionEventPartialClosed1m=n(eventSummary.positionEventPartialClosed1m,n(nextSummary.positionEventPartialClosed1m,0));nextSummary.positionEventClosed1m=n(eventSummary.positionEventClosed1m,n(nextSummary.positionEventClosed1m,0));nextSummary.positionEventDetectionP95Ms1m=n(eventSummary.positionEventDetectionP95Ms1m,n(nextSummary.positionEventDetectionP95Ms1m,NaN));nextSummary.positionEventFeedPath=String(eventSummary.positionEventFeedPath||"/api/live-trades/wallet-first/positions?feed_mode=events").trim()||"/api/live-trades/wallet-first/positions?feed_mode=events";nextPayload.positionEventsMeta={generatedAt:n(eventSummaryPayload.generatedAt,0),summary:eventSummary}}nextPayload.summary=nextSummary;nextPayload.generatedAt=Math.max(0,n(basePayload===null||basePayload===void 0?void 0:basePayload.generatedAt,0),n(positionsPayload===null||positionsPayload===void 0?void 0:positionsPayload.generatedAt,0),n(eventSummaryPayload===null||eventSummaryPayload===void 0?void 0:eventSummaryPayload.generatedAt,0));return nextPayload}function pageWindowFromSummary(key,rowsLength){const isPositions=key==="positions";const config=state.pagination[key]||{page:1,pageSize:isPositions?20:100};const pageSize=isPositions?Math.max(20,Math.min(20,Math.floor(n(config.pageSize,20)))):Math.max(10,Math.min(5000,Math.floor(n(config.pageSize,100))));const payload=state.payload||{};const summary=payload.summary||{};const feedMode=String(summary.positionFeedMode||payload.positionFeedMode||"").trim().toLowerCase();const totalRows=key==="positions"?feedMode&&feedMode!=="current"&&Number.isFinite(n(summary.positionEventsTotal,NaN))&&n(summary.positionEventsTotal,0)>0?n(summary.positionEventsTotal,rowsLength):Number.isFinite(n(summary.openPositionsTotal,NaN))&&n(summary.openPositionsTotal,0)>0?n(summary.openPositionsTotal,rowsLength):Number.isFinite(n(summary.positionEventsTotal,NaN))&&n(summary.positionEventsTotal,0)>0?n(summary.positionEventsTotal,rowsLength):Number.isFinite(n(summary.publicTradesTotal,NaN))?n(summary.publicTradesTotal,rowsLength):rowsLength:Number.isFinite(n(summary.indexedWalletsTotal,NaN))?n(summary.indexedWalletsTotal,rowsLength):rowsLength;const totalPages=Math.max(1,Math.ceil(totalRows/pageSize));const page=Math.min(Math.max(1,Math.floor(n(config.page,1))),totalPages);config.page=page;config.pageSize=pageSize;state.pagination[key]=config;return{totalRows,totalPages,page,pageSize,start:totalRows>0?(page-1)*pageSize+1:0,end:totalRows>0?Math.min(page*pageSize,totalRows):0}}function paginateRows(rows,key){const list=Array.isArray(rows)?rows:[];const isPositions=key==="positions";const config=state.pagination[key]||{page:1,pageSize:isPositions?20:100};const pageSize=isPositions?Math.max(20,Math.min(20,Math.floor(n(config.pageSize,20)))):Math.max(10,Math.min(5000,Math.floor(n(config.pageSize,100))));const totalRows=list.length;const totalPages=Math.max(1,Math.ceil(totalRows/pageSize));const page=Math.min(Math.max(1,Math.floor(n(config.page,1))),totalPages);const startIndex=totalRows>0?(page-1)*pageSize:0;const endIndex=Math.min(startIndex+pageSize,totalRows);const pageRows=totalRows>0?list.slice(startIndex,endIndex):[];config.pageSize=pageSize;config.page=page;state.pagination[key]=config;return{rows:pageRows,totalRows,totalPages,page,pageSize,start:totalRows>0?startIndex+1:0,end:endIndex}}function renderPagination(key,pageData){const info=el(`lt-${key}-page-info`);const windowNode=el(`lt-${key}-page-window`);const prev=el(`lt-${key}-page-prev`);const next=el(`lt-${key}-page-next`);if(!info||!windowNode||!prev||!next)return;info.textContent=`Page ${fmtInt(pageData.page)} / ${fmtInt(pageData.totalPages)}`;windowNode.textContent=`${fmtInt(pageData.start)}-${fmtInt(pageData.end)} of ${fmtInt(pageData.totalRows)}`;prev.disabled=pageData.page<=1;next.disabled=pageData.page>=pageData.totalPages}function applyPositionFilters(rows){const q=state.search.trim().toLowerCase();const f=state.filters;const walletNeedle=String(f.wallet||"").trim().toLowerCase();return rows.filter(row=>{if(!matchesSearch(row,q))return false;if(walletNeedle){const walletFields=[row.wallet,row.walletLabel,row.walletTitle].join(" ").toLowerCase();if(!walletFields.includes(walletNeedle))return false}if(f.freshness&&String(row.freshness||row.status||"")!==f.freshness)return false;const usdValue=Number.isFinite(n(row.eventUsd,NaN))?row.eventUsd:row.positionUsd;const pnlValue=Number.isFinite(n(row.unrealizedPnlUsd,NaN))?row.unrealizedPnlUsd:row.pnl;if(!withinRange(usdValue,f.minUsd,f.maxUsd))return false;if(!withinRange(pnlValue,f.minPnl,f.maxPnl))return false;if(f.trackedOnly&&!row.trackedWallet)return false;return true})}function applyWalletFilters(rows){const q=state.search.trim().toLowerCase();const f=state.filters;const walletNeedle=String(f.wallet||"").trim().toLowerCase();return rows.filter(row=>{if(!matchesSearch(row,q))return false;if(walletNeedle){const walletFields=[row.wallet,row.walletLabel,row.walletTitle].join(" ").toLowerCase();if(!walletFields.includes(walletNeedle))return false}if(f.freshness&&String(row.freshness||row.status||"")!==f.freshness)return false;if(f.trackedOnly&&!row.trackedWallet)return false;return true})}function badge(type,value){return`<span class="lt-badge ${escapeHtml(type)}">${escapeHtml(String(value||"-"))}</span>`}function renderActivityRail(level,tone){const active=clamp(level,0,5);return`<span class="lt-activity-rail">${Array.from({length:5},(_,idx)=>{const on=idx<active;return`<span class="${on?`is-on ${escapeHtml(tone||"cooling")}`:""}"></span>`}).join("")}</span>`}function renderScorePill(label,score,options={}){const inverse=Boolean(options.inverse);const tone=scoreTone(score,inverse);const numeric=n(score,NaN);const text=Number.isFinite(numeric)?`${label} ${fmtInt(numeric)}`:`${label} -`;return`<span class="lt-score-pill ${escapeHtml(tone)}">${escapeHtml(text)}</span>`}function renderMiniBars(values){const numbers=safeArray(values).map(value=>n(value,0));const maxAbs=Math.max(1,...numbers.map(value=>Math.abs(value)));return`<span class="lt-mini-bars">${numbers.map(value=>{const height=Math.max(8,Math.round(Math.abs(value)/maxAbs*28));const tone=value>=0?"up":"down";return`<span class="${tone}" style="height:${height}px"></span>`}).join("")}</span>`}function renderWalletCell(row,options={}){const walletText=row.walletLabel||shortWallet(row.wallet)||"Unattributed Flow";const showTrackedBadge=options.showTrackedBadge!==false;const showFreshnessBadge=options.showFreshnessBadge!==false;const showCopyButton=Boolean(options.showCopyButton);const statusClass=row.walletResolved?row.freshness==="fresh"?"fresh":row.freshness==="stale"?"stale":"cool":"warn";const statusLabel=row.walletResolved?row.freshness||"unknown":"unresolved";const tracked=showTrackedBadge&&row.trackedWallet?badge("tracked","tracked"):"";const freshness=showFreshnessBadge?badge(statusClass,statusLabel):"";const walletFilterValue=row.walletResolved?row.wallet:"";const walletButtonClass=row.walletResolved?"lt-wallet-link":"lt-wallet-link lt-wallet-link-disabled";const copyButton=showCopyButton&&row.walletResolved?`<button class="lt-inline-copy" type="button" data-copy-wallet="${escapeHtml(row.wallet)}" title="Copy wallet">Copy</button>`:"";const metaBits=[tracked,freshness].filter(Boolean).join("");return`<div class="lt-wallet-cell">
    <div class="lt-wallet-main">
      <button class="${walletButtonClass}" data-filter-wallet="${escapeHtml(walletFilterValue)}" title="${escapeHtml(row.walletTitle||row.wallet||walletText)}" ${row.walletResolved?"":"disabled"}>${escapeHtml(walletText)}</button>
      ${copyButton}
    </div>
    ${metaBits?`<div class="lt-wallet-meta">${metaBits}</div>`:""}
  </div>`}function attachWalletCopyButtons(root){if(!root)return;root.querySelectorAll("[data-copy-wallet]").forEach(button=>{button.addEventListener("click",async event=>{event.preventDefault();event.stopPropagation();const walletValue=String(button.getAttribute("data-copy-wallet")||"").trim();if(!walletValue)return;const original=button.textContent||"Copy";try{const copied=await copyTextToClipboard(walletValue);button.textContent=copied?"Copied":"Copy failed"}catch(_error){button.textContent="Copy failed"}window.setTimeout(()=>{button.textContent=original},1400)})})}function scrollDetailPanelIntoView(){const panel=el("lt-detail-panel");if(!panel||panel.hidden)return;panel.scrollIntoView({behavior:"smooth",block:"start"});window.requestAnimationFrame(()=>{if(panel instanceof HTMLElement)panel.focus({preventScroll:true})})}async function fetchJsonWithTimeout(url,timeoutMs=SNAPSHOT_TIMEOUT_MS,externalSignal=null){const controller=typeof AbortController==="function"?new AbortController:null;const abortFromExternal=()=>{if(controller)controller.abort()};if(externalSignal&&controller){if(externalSignal.aborted)controller.abort();else externalSignal.addEventListener("abort",abortFromExternal,{once:true})}const timeoutId=controller?setTimeout(()=>{try{controller.abort()}catch(_error){}},timeoutMs):null;try{const res=await fetch(url,{cache:"no-store",headers:{Accept:"application/json"},signal:controller?controller.signal:undefined});if(!res.ok){let message=`HTTP ${res.status}`;try{const body=await res.json();if(body&&body.error)message=String(body.error)}catch(_error){}throw new Error(message)}return await res.json()}finally{if(timeoutId)clearTimeout(timeoutId);if(externalSignal&&controller){externalSignal.removeEventListener("abort",abortFromExternal)}}}function renderPositionEventSummary(payload){var _asObject;const summary=asObject(payload===null||payload===void 0?void 0:payload.summary);const metrics=((_asObject=asObject(state.metrics))===null||_asObject===void 0?void 0:_asObject.aggregate)||{};const feedMode=String(summary.positionFeedMode||(payload===null||payload===void 0?void 0:payload.positionFeedMode)||"").trim().toLowerCase();const useEventsTotal=feedMode&&feedMode!=="current";const total=useEventsTotal&&Number.isFinite(n(summary.positionEventsTotal,NaN))?n(summary.positionEventsTotal,extractPositions(payload).length):Number.isFinite(n(summary.openPositionsTotal,NaN))?n(summary.openPositionsTotal,extractPositions(payload).length):n(summary.positionEventsTotal,extractPositions(payload).length);const feedPath=String(summary.positionEventFeedPath||"/api/live-trades/wallet-first/positions?feed_mode=events").trim();const scope=String(summary.streamScope||"wallet_first_tracked_wallet_positions").trim();const openedPerMinute=n(metrics.positionOpenedEventsPerMinute,n(metrics.newOpenEventsPerMinute,NaN));const increasedPerMinute=n(metrics.positionIncreasedEventsPerMinute,NaN);const partialPerMinute=n(metrics.positionPartialCloseEventsPerMinute,NaN);const closedPerMinute=n(metrics.positionClosedEventsPerMinute,NaN);const positionsPerMinute=[openedPerMinute,increasedPerMinute,partialPerMinute,closedPerMinute].map(value=>Number.isFinite(value)?value:0).reduce((sum,value)=>sum+value,0);const eventsPerMinute=n(metrics.eventsPerMinute,NaN);const speedText=Number.isFinite(positionsPerMinute)?`${fmtInt(positionsPerMinute)} position-events/min`:Number.isFinite(eventsPerMinute)?`${fmtInt(eventsPerMinute)} events/min`:"positions/min -";const throughputPill=el("lt-stage-throughput");if(throughputPill){throughputPill.textContent=`${fmtInt(total)} positions • ${speedText}`;const detailParts=[];if(Number.isFinite(openedPerMinute))detailParts.push(`opened ${fmtInt(openedPerMinute)}/min`);if(Number.isFinite(increasedPerMinute))detailParts.push(`increased ${fmtInt(increasedPerMinute)}/min`);if(Number.isFinite(partialPerMinute))detailParts.push(`partial ${fmtInt(partialPerMinute)}/min`);if(Number.isFinite(closedPerMinute))detailParts.push(`closed ${fmtInt(closedPerMinute)}/min`);throughputPill.title=[scope,feedPath,detailParts.join(" \u2022 ")].filter(Boolean).join(" \u2022 ")}}function renderPositionSortButton(key,label){const active=state.positionsSortKey===key;const indicator=active?state.positionsSortDir==="asc"?"\u2191":"\u2193":"\u2191\u2193";return`<button class="lt-position-sort-btn${active?" active":""}" type="button" data-position-sort-key="${escapeHtml(key)}">${escapeHtml(label)} <span>${indicator}</span></button>`}function renderPositionsTable(rows,payload=state.payload){const board=el("lt-position-events-board");if(!board)return;const confirmedRows=safeArray(extractPositions(payload));const confirmedByKey=new Map;confirmedRows.forEach(row=>{const key=[String((row===null||row===void 0?void 0:row.wallet)||"").trim(),String((row===null||row===void 0?void 0:row.symbol)||"").trim().toUpperCase(),String((row===null||row===void 0?void 0:row.side)||(row===null||row===void 0?void 0:row.rawSide)||"").trim().toLowerCase(),String((row===null||row===void 0?void 0:row.positionKey)||(row===null||row===void 0?void 0:row.key)||"").trim()].filter(Boolean).join("|");if(key)confirmedByKey.set(key,row)});const resolveConfirmedRow=row=>{const key=[String((row===null||row===void 0?void 0:row.wallet)||"").trim(),String((row===null||row===void 0?void 0:row.symbol)||"").trim().toUpperCase(),String((row===null||row===void 0?void 0:row.side)||(row===null||row===void 0?void 0:row.rawSide)||"").trim().toLowerCase(),String((row===null||row===void 0?void 0:row.positionKey)||(row===null||row===void 0?void 0:row.key)||"").trim()].filter(Boolean).join("|");return key&&confirmedByKey.get(key)||null};const pickPositiveValue=values=>values.find(value=>Number.isFinite(value)&&Math.abs(value)>0);const visibleRows=safeArray(rows).filter(row=>{const rowEventType=String((row===null||row===void 0?void 0:row.eventType)||(row===null||row===void 0?void 0:row.sideEvent)||(row===null||row===void 0?void 0:row.cause)||(row===null||row===void 0?void 0:row.type)||"").trim().toLowerCase();const lifecycleRow=Boolean(rowEventType);const confirmed=resolveConfirmedRow(row);const sizeValue=pickPositiveValue([n(row===null||row===void 0?void 0:row.size,NaN),n(row===null||row===void 0?void 0:row.currentSize,NaN),n(row===null||row===void 0?void 0:row.newSize,NaN),n(row===null||row===void 0?void 0:row.sizeDeltaAbs,NaN),confirmed?n(confirmed.size,NaN):NaN,confirmed?n(confirmed.currentSize,NaN):NaN,confirmed?n(confirmed.newSize,NaN):NaN])||(()=>{const directEntry=pickPositiveValue([n(row===null||row===void 0?void 0:row.entry,NaN),n(row===null||row===void 0?void 0:row.entryPrice,NaN),n(row===null||row===void 0?void 0:row.entry_price,NaN),n(row===null||row===void 0?void 0:row.price,NaN),confirmed?n(confirmed.entry,NaN):NaN,confirmed?n(confirmed.entryPrice,NaN):NaN,confirmed?n(confirmed.entry_price,NaN):NaN,confirmed?n(confirmed.price,NaN):NaN]);const directPositionUsd=pickPositiveValue([n(row===null||row===void 0?void 0:row.positionUsd,NaN),n(row===null||row===void 0?void 0:row.eventUsd,NaN),n(row===null||row===void 0?void 0:row.notionalUsd,NaN),confirmed?n(confirmed.positionUsd,NaN):NaN,confirmed?n(confirmed.eventUsd,NaN):NaN,confirmed?n(confirmed.notionalUsd,NaN):NaN]);if(Number.isFinite(directEntry)&&Number.isFinite(directPositionUsd)&&directEntry>0&&directPositionUsd>0){return directPositionUsd/directEntry}return 0})();return sizeValue>0||lifecycleRow});const columns=[{key:"wallet",label:"Wallet address",align:"left",sortKey:"wallet"},{key:"symbol",label:"Token",align:"left",sortKey:"symbol"},{key:"side",label:"Side",align:"left",sortKey:"side"},{key:"entry",label:"Entry price",align:"right",sortKey:"entry"},{key:"size",label:"Size",align:"right",sortKey:"size"},{key:"position",label:"Position size",align:"right",sortKey:"positionUsd"},{key:"leverage",label:"Leverage",align:"right"},{key:"margin",label:"Margin",align:"right"},{key:"opened",label:"Time opened",align:"left",sortKey:"openedAt"}];const headerRow=`<tr>${columns.map(col=>`<th class="${col.align==="right"?"right":"left"}" data-col="${escapeHtml(col.key)}">${col.sortKey?renderPositionSortButton(col.sortKey,col.label):escapeHtml(col.label)}</th>`).join("")}</tr>`;const bodyRows=visibleRows.map(row=>{var _row$positionState;const confirmed=resolveConfirmedRow(row);const openedAt=row.openedAt||row.detectedAt||row.updatedAt||row.timestamp||0;const rowSide=String(row.side||row.rawSide||"").trim().toLowerCase();const sideText=rowSide.replaceAll("_"," ").replaceAll("-"," ").trim();const sideIsLong=sideText==="long"||sideText.includes(" long")||sideText.startsWith("long ")||sideText==="buy";const sideIsShort=sideText==="short"||sideText.includes(" short")||sideText.startsWith("short ")||sideText==="sell";const sideLabel=rowSide?titleCase(rowSide):"-";const sideClass=sideIsLong?"side-long":sideIsShort?"side-short":"side-neutral";const sizeValue=pickPositiveValue([n(row.size,NaN),n(row.currentSize,NaN),n(row.newSize,NaN),n(row.sizeDeltaAbs,NaN),confirmed?n(confirmed.size,NaN):NaN,confirmed?n(confirmed.currentSize,NaN):NaN,confirmed?n(confirmed.newSize,NaN):NaN]);const positionSizeValue=pickPositiveValue([n(row.positionUsd,NaN),n(row.eventUsd,NaN),n(row.notionalUsd,NaN),confirmed?n(confirmed.positionUsd,NaN):NaN,confirmed?n(confirmed.eventUsd,NaN):NaN,confirmed?n(confirmed.notionalUsd,NaN):NaN])||(Number.isFinite(sizeValue)&&Number.isFinite(n(row.entry,NaN))&&Math.abs(n(row.entry,0))>0?Math.abs(sizeValue)*Math.abs(n(row.entry,0)):NaN);const entryValue=pickPositiveValue([n(row.entry,NaN),n(row.entryPrice,NaN),n(row.entry_price,NaN),n(row.price,NaN),confirmed?n(confirmed.entry,NaN):NaN,confirmed?n(confirmed.entryPrice,NaN):NaN,confirmed?n(confirmed.entry_price,NaN):NaN,confirmed?n(confirmed.price,NaN):NaN])||(Number.isFinite(sizeValue)&&Number.isFinite(positionSizeValue)&&Math.abs(sizeValue)>0?Math.abs(positionSizeValue)/Math.abs(sizeValue):NaN);let marginValue=pickPositiveValue([n(row.margin,NaN),n(row.marginUsd,NaN),n(row.margin_usd,NaN),confirmed?n(confirmed.margin,NaN):NaN,confirmed?n(confirmed.marginUsd,NaN):NaN,confirmed?n(confirmed.margin_usd,NaN):NaN])||NaN;let leverageValue=pickPositiveValue([n(row.leverage,NaN),n(row.lev,NaN),n(row.leverageValue,NaN),n(row.leverage_value,NaN),confirmed?n(confirmed.leverage,NaN):NaN,confirmed?n(confirmed.lev,NaN):NaN,confirmed?n(confirmed.leverageValue,NaN):NaN,confirmed?n(confirmed.leverage_value,NaN):NaN,Number.isFinite(marginValue)&&marginValue>0&&Number.isFinite(positionSizeValue)?positionSizeValue/marginValue:NaN])||NaN;const isolatedFlag=(()=>{if(Object.prototype.hasOwnProperty.call(row||{},"isolated")){return Boolean(row.isolated)}if(row!==null&&row!==void 0&&row.raw&&typeof row.raw==="object"&&Object.prototype.hasOwnProperty.call(row.raw,"isolated")){return Boolean(row.raw.isolated)}if(Object.prototype.hasOwnProperty.call(confirmed||{},"isolated")){return Boolean(confirmed.isolated)}if(confirmed!==null&&confirmed!==void 0&&confirmed.raw&&typeof confirmed.raw==="object"&&Object.prototype.hasOwnProperty.call(confirmed.raw,"isolated")){return Boolean(confirmed.raw.isolated)}return null})();if(!Number.isFinite(leverageValue)&&Number.isFinite(marginValue)&&marginValue>0&&Number.isFinite(positionSizeValue)&&positionSizeValue>0){leverageValue=positionSizeValue/marginValue}if(!Number.isFinite(marginValue)&&Number.isFinite(leverageValue)&&leverageValue>0&&Number.isFinite(positionSizeValue)&&positionSizeValue>0){marginValue=positionSizeValue/leverageValue}if(!Number.isFinite(leverageValue)&&Number.isFinite(positionSizeValue)&&positionSizeValue>0){leverageValue=1}if(!Number.isFinite(marginValue)&&Number.isFinite(positionSizeValue)&&positionSizeValue>=0){const divisor=Number.isFinite(leverageValue)&&leverageValue>0?leverageValue:1;marginValue=positionSizeValue/divisor;leverageValue=divisor}if(!Number.isFinite(marginValue))marginValue=0;if(!Number.isFinite(leverageValue)||leverageValue<=0)leverageValue=1;const leverageText=`${fmtNum(leverageValue,2)}x`;const marginText=fmtUsd(marginValue,2);const isNewlySeen=Boolean(row.isNewlySeen);const rowClass=["lt-position-row",sideClass,isNewlySeen?"is-new":"",(_row$positionState=row.positionState)!==null&&_row$positionState!==void 0&&_row$positionState.tone?`tone-${escapeHtml(String(row.positionState.tone).trim().toLowerCase())}`:""].filter(Boolean).join(" ");return`<tr class="${rowClass}">
          <td data-col="wallet">${renderWalletCell(row,{showTrackedBadge:false,showFreshnessBadge:false,showCopyButton:true})}</td>
          <td data-col="symbol">${escapeHtml(row.symbol||"-")}</td>
          <td data-col="side"><span class="lt-position-side-pill ${escapeHtml(sideClass)}">${escapeHtml(sideLabel)}</span></td>
          <td data-col="entry" class="right">${escapeHtml(Number.isFinite(entryValue)?fmtNum(entryValue,4):"-")}</td>
          <td data-col="size" class="right">${escapeHtml(Number.isFinite(sizeValue)?fmtNum(sizeValue,4):"-")}</td>
          <td data-col="position" class="right">${escapeHtml(Number.isFinite(positionSizeValue)?fmtUsd(positionSizeValue,2):"-")}</td>
          <td data-col="leverage" class="right">${escapeHtml(leverageText)}</td>
          <td data-col="margin" class="right">${escapeHtml(marginText)}</td>
      <td data-col="opened" title="${escapeHtml(openedAt?fmtTs(openedAt):"-")}">${escapeHtml(openedAt?fmtTs(openedAt):"-")}${isNewlySeen?"<span class=\"lt-position-new-pill\">NEW</span>":""}</td>
        </tr>`}).join("");board.innerHTML=`<div class="lt-table-wrap lt-table-wrap-positions lt-position-single-surface">
    <table id="lt-positions-table">
      <thead>${headerRow}</thead>
      <tbody>${bodyRows||`<tr><td colspan="9" class="lt-empty-state">No recent position events yet.</td></tr>`}</tbody>
    </table>
  </div>`;board.querySelectorAll("[data-position-sort-key]").forEach(button=>{button.addEventListener("click",()=>{const nextKey=String(button.getAttribute("data-position-sort-key")||"").trim();if(!nextKey)return;if(state.positionsSortKey===nextKey){state.positionsSortDir=state.positionsSortDir==="asc"?"desc":"asc"}else{state.positionsSortKey=nextKey;state.positionsSortDir="desc"}state.pagination.positions.page=1;fetchSnapshot().then(nextPayload=>applyPayload(nextPayload,"sort")).catch(()=>{});connectSse()})});board.querySelectorAll("button.lt-wallet-link[data-filter-wallet]").forEach(btn=>{btn.addEventListener("click",event=>{event.stopPropagation();const walletValue=String(btn.getAttribute("data-filter-wallet")||"").trim();if(!walletValue)return;const walletInput=el("lt-filter-wallet");if(walletInput)walletInput.value=walletValue;state.filters.wallet=walletValue;resetPagination();render()})});attachWalletCopyButtons(board)}function renderWalletsTable(rows){const head=el("lt-wallets-head");const body=el("lt-wallets-body");if(!head||!body)return;const columns=[{key:"wallet",label:"Wallet",align:"left"},{key:"pnl7",label:"Realized 7D",align:"right",sortKey:"pnl7"},{key:"pnl30",label:"Realized 30D",align:"right",sortKey:"pnl30"},{key:"pnlAll",label:"Realized All",align:"right",sortKey:"pnlAll"},{key:"open",label:"Open Positions",align:"right"},{key:"activity",label:"Last Trade",align:"left"}];head.innerHTML=`<tr>${columns.map(col=>`<th class="${col.align==="right"?"right":"left"}" data-col="${escapeHtml(col.key)}" aria-sort="${col.sortKey&&state.walletsSortKey===col.sortKey?state.walletsSortDir==="asc"?"ascending":"descending":"none"}">${col.sortKey?`<button class="lt-table-sort-btn${state.walletsSortKey===col.sortKey?" active":""}" type="button" data-wallet-sort-key="${escapeHtml(col.sortKey)}" data-wallet-sort-label="${escapeHtml(col.label)}">${escapeHtml(col.label)} <span data-wallet-sort-indicator>${state.walletsSortKey===col.sortKey?state.walletsSortDir==="asc"?"\u2191":"\u2193":"\u2191\u2193"}</span></button>`:escapeHtml(col.label)}</th>`).join("")}</tr>`;head.querySelectorAll("[data-wallet-sort-key]").forEach(button=>{button.addEventListener("click",()=>{const nextKey=String(button.getAttribute("data-wallet-sort-key")||"").trim();if(!nextKey)return;if(state.walletsSortKey===nextKey){state.walletsSortDir=state.walletsSortDir==="asc"?"desc":"asc"}else{state.walletsSortKey=nextKey;state.walletsSortDir="desc"}state.pagination.wallets.page=1;fetchSnapshot().then(payload=>applyPayload(payload,"sort")).catch(()=>{});connectSse()})});if(!rows.length){body.innerHTML="<tr><td colspan=\"6\" class=\"lt-empty-state\">No wallets match the current filters.</td></tr>";return}body.innerHTML=rows.map(row=>{const isSelected=state.selection.type==="wallets"&&state.selection.key===row.key;const freshClass=row.freshness==="fresh"?"fresh":row.freshness==="stale"?"stale":row.freshness==="unresolved"?"warn":"cool";const liveRankText=row.liveActiveRank?`#${fmtInt(row.liveActiveRank)} live`:"tracked wallet";const realized7=Number.isFinite(n(row.pnl7Usd,NaN))?fmtSignedUsd(row.pnl7Usd,2):"n/a";const realized30=Number.isFinite(n(row.pnl30Usd,NaN))?fmtSignedUsd(row.pnl30Usd,2):"n/a";const realizedAll=Number.isFinite(n(row.realizedPnlUsd,NaN))?fmtSignedUsd(row.realizedPnlUsd,2):"n/a";const lastActivity=row.lastActivityAt||row.lastActivity||row.lastTrade?fmtAgo(row.lastActivityAt||row.lastActivity||row.lastTrade):"-";return`<tr class="${isSelected?"is-selected":""}" data-select-type="wallets" data-select-key="${escapeHtml(row.key)}" tabindex="0" aria-selected="${isSelected?"true":"false"}">
        <td class="left" data-col="wallet">
          <div class="lt-wallet-cell">
            ${renderWalletCell(row)}
            <div class="lt-wallet-meta">
              <span>${escapeHtml(liveRankText)}</span>
              <span>${escapeHtml(fmtInt(row.openPositions||0))} open</span>
            </div>
          </div>
        </td>
        <td class="right mono ${n(row.pnl7Usd,0)>=0?"up":"down"}" data-col="pnl7">${escapeHtml(realized7)}</td>
        <td class="right mono ${n(row.pnl30Usd,0)>=0?"up":"down"}" data-col="pnl30">${escapeHtml(realized30)}</td>
        <td class="right mono ${n(row.realizedPnlUsd,0)>=0?"up":"down"}" data-col="pnlAll">${escapeHtml(realizedAll)}</td>
        <td class="right mono" data-col="open">${escapeHtml(fmtInt(row.openPositions||0))}</td>
        <td class="left" data-col="activity">
          <div class="lt-activity-cell">
            <div class="lt-activity-meta">
              ${badge(freshClass,row.freshness||"unknown")}
              <span>${escapeHtml(lastActivity)}</span>
            </div>
            <div class="lt-metric-subrow">
              ${row.liveActiveRank?`<span>#${escapeHtml(fmtInt(row.liveActiveRank))} live</span>`:"<span>tracked</span>"}
              <span>${escapeHtml(fmtInt(row.recentEvents15m||0))} ev / 15m</span>
            </div>
          </div>
        </td>
      </tr>`}).join("");body.querySelectorAll("button.lt-wallet-link[data-filter-wallet]").forEach(btn=>{btn.addEventListener("click",event=>{event.stopPropagation();const walletValue=String(btn.getAttribute("data-filter-wallet")||"").trim();if(!walletValue)return;const walletInput=el("lt-filter-wallet");if(walletInput)walletInput.value=walletValue;state.filters.wallet=walletValue;resetPagination();render()})});attachWalletCopyButtons(body);body.querySelectorAll("tr[data-select-type='wallets'][data-select-key]").forEach(rowNode=>{const selectRow=event=>{const button=event.target.closest("button.lt-wallet-link");if(button)return;const row=rows.find(entry=>entry.key===String(rowNode.getAttribute("data-select-key")||""));state.selection={type:"wallets",key:String(rowNode.getAttribute("data-select-key")||"")};if(row&&row.walletResolved){openWalletDrawerForRow(row)}else{render()}};rowNode.addEventListener("click",selectRow);rowNode.addEventListener("keydown",event=>{if(event.key!=="Enter"&&event.key!==" ")return;event.preventDefault();selectRow(event)})})}function sortWalletRows(rows){const list=safeArray(rows).slice();const key=String(state.walletsSortKey||"pnlAll").trim();const dir=state.walletsSortDir==="asc"?"asc":"desc";const numericValue=(row,sortKey)=>{if(sortKey==="pnl7")return n(row===null||row===void 0?void 0:row.pnl7Usd,NaN);if(sortKey==="pnl30")return n(row===null||row===void 0?void 0:row.pnl30Usd,NaN);if(sortKey==="pnlAll")return n(row===null||row===void 0?void 0:row.realizedPnlUsd,NaN);return NaN};list.sort((left,right)=>{const leftVal=numericValue(left,key);const rightVal=numericValue(right,key);const leftMissing=!Number.isFinite(leftVal);const rightMissing=!Number.isFinite(rightVal);if(leftMissing&&rightMissing){return tsMs(right===null||right===void 0?void 0:right.lastActivity)-tsMs(left===null||left===void 0?void 0:left.lastActivity)}if(leftMissing)return 1;if(rightMissing)return-1;if(leftVal===rightVal){return tsMs(right===null||right===void 0?void 0:right.lastActivity)-tsMs(left===null||left===void 0?void 0:left.lastActivity)}return dir==="asc"?leftVal-rightVal:rightVal-leftVal});return list}function renderActiveView(){const activeView=state.activeView==="wallets"?"wallets":"positions";const inactiveView=activeView==="positions"?"wallets":"positions";const activePanel=el(`lt-view-${activeView}`);const inactivePanel=el(`lt-view-${inactiveView}`);const activeButton=el(`lt-view-${activeView}-btn`);const inactiveButton=el(`lt-view-${inactiveView}-btn`);const config=VIEW_CONFIG[activeView]||VIEW_CONFIG.positions;if(activePanel){activePanel.hidden=false;activePanel.classList.add("is-active")}if(inactivePanel){inactivePanel.hidden=true;inactivePanel.classList.remove("is-active")}if(activeButton){activeButton.classList.add("active");activeButton.setAttribute("aria-selected","true")}if(inactiveButton){inactiveButton.classList.remove("active");inactiveButton.setAttribute("aria-selected","false")}const kicker=el("lt-stage-kicker");if(kicker)kicker.textContent=config.kicker;const title=el("lt-stage-title");if(title)title.textContent=config.title;const minUsdLabel=el("lt-filter-min-usd-label");const maxUsdLabel=el("lt-filter-max-usd-label");const minPnlLabel=el("lt-filter-min-pnl-label");const maxPnlLabel=el("lt-filter-max-pnl-label");if(minUsdLabel){minUsdLabel.textContent=activeView==="positions"?"Min Event USD":"Min Total Volume USD"}if(maxUsdLabel){maxUsdLabel.textContent=activeView==="positions"?"Max Event USD":"Max Total Volume USD"}if(minPnlLabel){minPnlLabel.textContent=activeView==="positions"?"Min PnL":"Min Total PnL"}if(maxPnlLabel){maxPnlLabel.textContent=activeView==="positions"?"Max PnL":"Max Total PnL"}document.querySelectorAll("[data-filter-scope='positions']").forEach(node=>{node.hidden=activeView!=="positions"})}function closeWalletDrawer(){if(state.drawer.abortController){try{state.drawer.abortController.abort()}catch(_error){}}state.drawer={wallet:"",loading:false,error:"",detail:null,sourceRow:null,requestId:state.drawer.requestId+1,abortController:null}}async function openWalletDrawerForRow(row){const wallet=normalizeWallet(row&&row.wallet);if(!wallet)return;if(state.drawer.abortController){try{state.drawer.abortController.abort()}catch(_error){}}const abortController=typeof AbortController==="function"?new AbortController:null;const requestId=state.drawer.requestId+1;state.drawer={wallet,loading:true,error:"",detail:null,sourceRow:row||null,requestId,abortController};renderDetailPanel();scrollDetailPanelIntoView();try{const payload=await fetchJsonWithTimeout(`/api/live-trades/wallet-first?wallet=${encodeURIComponent(wallet)}&wallet_limit=1&position_limit=200&live_active_limit=1`,DRAWER_FETCH_TIMEOUT_MS,abortController?abortController.signal:null);if(requestId!==state.drawer.requestId)return;state.drawer={wallet,loading:false,error:"",detail:payload,sourceRow:row||null,requestId,abortController:null}}catch(error){if(abortController&&abortController.signal.aborted)return;if(requestId!==state.drawer.requestId)return;state.drawer={wallet,loading:false,error:error&&error.message?error.message:"Failed to load wallet detail.",detail:null,sourceRow:row||null,requestId,abortController:null}}renderDetailPanel();scrollDetailPanelIntoView()}function renderWalletPositionsList(positions,sourceRow){const rows=safeArray(positions);if(!rows.length){return"<div class=\"lt-drawer-empty\">No open positions currently tracked for this wallet.</div>"}return`<div class="lt-drawer-positions-wrap">
    <table class="lt-drawer-positions-table">
      <thead>
        <tr>
          <th>Symbol</th>
          <th>Side</th>
          <th class="right">Position USD</th>
          <th class="right">Entry</th>
          <th class="right">Mark</th>
          <th>Updated</th>
        </tr>
      </thead>
      <tbody>
        ${rows.map(row=>{const selected=sourceRow&&sourceRow.tradeRef&&row.id&&String(sourceRow.tradeRef)===String(row.id);const side=normalizeSide(row.side);const sideClass=side.includes("long")||side==="buy"?"up":side.includes("short")||side==="sell"?"down":"flat";return`<tr${selected?" class=\"is-focus\"":""}>
              <td><strong>${escapeHtml(row.symbol||"-")}</strong></td>
              <td>${badge(sideClass,titleCase(row.side||"-"))}</td>
              <td class="right mono">${escapeHtml(fmtUsd(row.positionUsd,2))}</td>
              <td class="right mono">${escapeHtml(Number.isFinite(n(row.entry,NaN))?fmtNum(row.entry,4):"-")}</td>
              <td class="right mono">${escapeHtml(Number.isFinite(n(row.mark,NaN))?fmtNum(row.mark,4):"-")}</td>
              <td>${escapeHtml(row.updatedAt?fmtAgo(row.updatedAt):"-")}</td>
            </tr>`}).join("")}
      </tbody>
    </table>
  </div>`}function renderAllocationBars(entries){const rows=safeArray(entries);if(!rows.length){return"<div class=\"lt-drawer-empty\">No concentration data available.</div>"}return`<div class="lt-allocation-list">
    ${rows.map(entry=>{const pct=clamp(entry.sharePct,0,100);return`<div class="lt-allocation-row">
          <div class="lt-allocation-label">
            <span class="lt-allocation-symbol">${escapeHtml(entry.symbol||"-")}</span>
            <span class="lt-allocation-values">
              <strong>${escapeHtml(fmtUsd(entry.volumeUsd,2))}</strong>
              <span>${escapeHtml(fmtPct(pct,1))}</span>
            </span>
          </div>
          <div class="lt-allocation-bar"><span style="width:${pct.toFixed(2)}%"></span></div>
        </div>`}).join("")}
  </div>`}function renderDetailPanel(){var _safeArray$;const panel=el("lt-detail-panel");const detailTitle=el("lt-detail-title");const detailSubtitle=el("lt-detail-subtitle");const detailKicker=el("lt-detail-kicker");const detailBody=el("lt-detail-body");if(!panel||!detailTitle||!detailSubtitle||!detailKicker||!detailBody)return;if(!state.drawer.wallet){panel.hidden=true;detailBody.innerHTML="";return}panel.hidden=false;detailKicker.textContent="Wallet detail";if(state.drawer.loading){var _state$drawer$sourceR,_state$drawer$sourceR2;detailTitle.textContent=((_state$drawer$sourceR=state.drawer.sourceRow)===null||_state$drawer$sourceR===void 0?void 0:_state$drawer$sourceR.walletTitle)||((_state$drawer$sourceR2=state.drawer.sourceRow)===null||_state$drawer$sourceR2===void 0?void 0:_state$drawer$sourceR2.walletLabel)||shortWallet(state.drawer.wallet);detailSubtitle.textContent="Loading wallet summary and open positions.";detailBody.innerHTML="<div class=\"lt-drawer-empty\">Loading wallet details...</div>";return}if(state.drawer.error){var _state$drawer$sourceR3,_state$drawer$sourceR4;detailTitle.textContent=((_state$drawer$sourceR3=state.drawer.sourceRow)===null||_state$drawer$sourceR3===void 0?void 0:_state$drawer$sourceR3.walletTitle)||((_state$drawer$sourceR4=state.drawer.sourceRow)===null||_state$drawer$sourceR4===void 0?void 0:_state$drawer$sourceR4.walletLabel)||shortWallet(state.drawer.wallet);detailSubtitle.textContent="Wallet detail unavailable";detailBody.innerHTML=`<div class="lt-drawer-empty">${escapeHtml(state.drawer.error)}</div>`;return}const detail=asObject(state.drawer.detail);const summary=asObject(detail.summary);const positions=safeArray(detail.positions);const sourceRow=state.drawer.sourceRow||null;detailTitle.textContent=(sourceRow===null||sourceRow===void 0?void 0:sourceRow.walletTitle)||(sourceRow===null||sourceRow===void 0?void 0:sourceRow.walletLabel)||shortWallet(detail.wallet||state.drawer.wallet);detailSubtitle.textContent=`${titleCase(summary.freshness||"unknown")} • ${summary.liveActiveRank?`#${fmtInt(summary.liveActiveRank)} live`:"tracked wallet"}`;const all=asObject(summary.all);const d7=asObject(summary.d7);const d30=asObject(summary.d30);const d24=asObject(summary.d24);const lifecycle=asObject(summary.lifecycle);const behaviorTags=deriveWalletBehaviorTags({recentEvents15m:summary.recentEvents15m,recentEvents1h:summary.recentEvents1h,openPositions:summary.openPositions,concentrationPct:((_safeArray$=safeArray(summary.concentration)[0])===null||_safeArray$===void 0?void 0:_safeArray$.sharePct)||0,winRate:all.winRatePct,pnl7Usd:d7.pnlUsd,totalPnlUsd:summary.totalPnlUsd,freshness:summary.freshness,increaseCount:lifecycle.increaseCount,reduceCount:lifecycle.reduceCount,avgHoldingMs:lifecycle.avgObservedHoldingMs||lifecycle.avgCompletedHoldingMs||lifecycle.currentAvgOpenAgeMs,totalTrades:all.trades});const walletAddress=detail.wallet||state.drawer.wallet;const shortAddress=shortWallet(walletAddress,8,6);const summaryParts=[];if(Number.isFinite(n(all.winRatePct,NaN))){summaryParts.push(`Win Rate ${fmtPct(all.winRatePct,2)}`)}if(Number.isFinite(n(all.trades,NaN))){summaryParts.push(`${fmtInt(all.trades)} trades`)}if(Number.isFinite(n(summary.exposureUsd,NaN))){summaryParts.push(`${fmtUsd(summary.exposureUsd,2)} exposure`)}detailBody.innerHTML=`
    <div class="lt-detail-header">
      <div class="lt-detail-identity">
        <span class="lt-detail-label">Wallet</span>
        <span class="lt-detail-address">${escapeHtml(shortAddress)}</span>
        <div class="lt-detail-actions">
          <button type="button" class="lt-inline-copy" data-copy-wallet="${escapeHtml(walletAddress)}">Copy</button>
        </div>
      </div>
      <div class="lt-detail-badges">
        ${badge(summary.freshness==="fresh"?"fresh":summary.freshness==="stale"?"stale":"cool",titleCase(summary.freshness||"unknown"))}
        ${badge("rank",`${fmtInt(summary.openPositions||positions.length)} open`)}
      </div>
    </div>
    ${summaryParts.length?`<div class="lt-detail-summary">${escapeHtml(summaryParts.join(" \u2022 "))}</div>`:""}
    <div class="lt-detail-kpi-grid">
      <div class="lt-detail-kpi-card">
        <span>24H Realized</span>
        <strong class="${n(d24.pnlUsd,0)>=0?"up":"down"}">${escapeHtml(fmtSignedUsd(d24.pnlUsd,2))}</strong>
      </div>
      <div class="lt-detail-kpi-card">
        <span>7D Realized</span>
        <strong class="${n(d7.pnlUsd,0)>=0?"up":"down"}">${escapeHtml(fmtSignedUsd(d7.pnlUsd,2))}</strong>
      </div>
      <div class="lt-detail-kpi-card">
        <span>30D Realized</span>
        <strong class="${n(d30.pnlUsd,0)>=0?"up":"down"}">${escapeHtml(fmtSignedUsd(d30.pnlUsd,2))}</strong>
      </div>
      <div class="lt-detail-kpi-card">
        <span>Total PnL</span>
        <strong class="${n(summary.totalPnlUsd,0)>=0?"up":"down"}">${escapeHtml(fmtSignedUsd(summary.totalPnlUsd||0,2))}</strong>
      </div>
    </div>
    <section class="lt-detail-section-block">
      <h4>Wallet posture</h4>
      <div class="lt-detail-section-grid">
        <div class="lt-detail-item">
          <div class="lt-detail-item-label">Open positions</div>
          <div class="lt-detail-item-value">${escapeHtml(fmtInt(summary.openPositions||positions.length))}</div>
        </div>
        <div class="lt-detail-item">
          <div class="lt-detail-item-label">Exposure</div>
          <div class="lt-detail-item-value">${escapeHtml(fmtUsd(summary.exposureUsd||0,2))}</div>
        </div>
        <div class="lt-detail-item">
          <div class="lt-detail-item-label">Wallet unrealized</div>
          <div class="lt-detail-item-value ${n(summary.unrealizedPnlUsd,0)>=0?"up":"down"}">${escapeHtml(fmtSignedUsd(summary.unrealizedPnlUsd||0,2))}</div>
        </div>
        <div class="lt-detail-item">
          <div class="lt-detail-item-label">Last trade</div>
          <div class="lt-detail-item-value">${escapeHtml(summary.lastActivityAt?fmtTs(summary.lastActivityAt):"-")}</div>
        </div>
        <div class="lt-detail-item">
          <div class="lt-detail-item-label">Recent events</div>
          <div class="lt-detail-item-value">${escapeHtml(fmtInt(summary.recentEvents15m||0))} / 15m</div>
        </div>
        <div class="lt-detail-item">
          <div class="lt-detail-item-label">Live score</div>
          <div class="lt-detail-item-value">${escapeHtml(fmtInt(summary.liveActiveScore||0))}</div>
        </div>
      </div>
    </section>
    <div class="lt-detail-columns">
      <section class="lt-detail-section-block">
        <h4>Open positions</h4>
        ${renderWalletPositionsList(positions,sourceRow)}
      </section>
      <section class="lt-detail-section-block">
        <h4>Concentration</h4>
        ${renderAllocationBars(summary.concentration)}
      </section>
    </div>
    <section class="lt-detail-section-block">
      <h4>Behavior context</h4>
      <div class="lt-tag-group">
        ${behaviorTags.length?behaviorTags.map(tag=>badge("cool",tag)).join(" "):"<span class=\"lt-drawer-empty\">No recent behavior signals.</span>"}
      </div>
    </section>
  `;attachWalletCopyButtons(detailBody)}function connectionState(){if(state.stream.paused)return{label:"PAUSED",className:"paused"};const heartbeatAge=state.stream.lastHeartbeatAt?Date.now()-state.stream.lastHeartbeatAt:Infinity;const snapshotAge=state.stream.lastSnapshotAt?Date.now()-state.stream.lastSnapshotAt:Infinity;const age=Math.min(heartbeatAge,snapshotAge);if(state.stream.connection==="open"&&age<=LIVE_DELAYED_MS)return{label:"LIVE",className:"live"};if(state.stream.connection==="open"&&age<=LIVE_STALE_MS)return{label:"DELAYED",className:"delayed"};if(state.stream.connection==="reconnecting")return{label:"RECONNECTING",className:"warn"};if(state.stream.connection==="fallback")return{label:"DEGRADED",className:"warn"};if(age>LIVE_STALE_MS)return{label:"STALE",className:"bad"};return{label:"IDLE",className:"idle"}}function renderHealth(payload){const status=connectionState();const liveBadge=el("lt-live-badge");if(liveBadge){liveBadge.textContent=status.label;liveBadge.className=`lt-pill lt-pill-live ${status.className}`}updateToolbarControls()}function updateToolbarControls(){const refreshBtn=el("lt-refresh-now-btn");if(refreshBtn){refreshBtn.disabled=Boolean(state.stream.positionsRequestInFlight)}const streamToggleBtn=el("lt-stream-toggle-btn");if(streamToggleBtn){const paused=Boolean(state.stream.paused);streamToggleBtn.textContent=paused?"Resume Stream":"Pause Stream";streamToggleBtn.setAttribute("aria-pressed",paused?"true":"false")}const updatedNode=el("lt-toolbar-updated");if(updatedNode){var _state$payload;const updatedAt=Math.max(tsMs(((_state$payload=state.payload)===null||_state$payload===void 0?void 0:_state$payload.generatedAt)||0),n(state.stream.lastSnapshotAt,0),n(state.stream.lastPayloadAt,0));updatedNode.textContent=updatedAt>0?`Updated ${fmtAgo(updatedAt)}`:"Updated -"}}async function refreshNow(source="manual_refresh"){const payload=await fetchSnapshot();applyPayload(payload,source);await refreshPositionsFeed(source);await refreshMetrics(source)}function pauseStream(){if(state.stream.paused)return;state.stream.paused=true;state.stream.connection="idle";stopEventSource();updateToolbarControls();renderHealth(state.payload||{})}function resumeStream(){if(!state.stream.paused)return;state.stream.paused=false;state.stream.connection="connecting";connectSse();ensureFallbackPolling();ensurePositionsPolling();ensureMetricsPolling();refreshPositionsFeed("resume").catch(()=>{});refreshMetrics("resume").catch(()=>{});updateToolbarControls();renderHealth(state.payload||{})}function toggleStreamState(){if(state.stream.paused){resumeStream();return}pauseStream()}function renderTopSummary(payload){const summary=asObject(payload===null||payload===void 0?void 0:payload.summary);const trackedWallets=n(summary.trackedWallets,payloadWalletsTotal(payload));const walletsWithPortfolioMetrics=n(summary.walletsWithPortfolioMetrics,0);const liveActiveWalletsTotal=n(summary.liveActiveWalletsTotal,n(summary.liveActiveWallets,0));const openPositionsTotal=n(summary.openPositionsTotal,payloadPositionsTotal(payload));const portfolioCoveragePct=trackedWallets>0?Math.min(100,walletsWithPortfolioMetrics/trackedWallets*100):NaN;const trackedNode=el("lt-summary-tracked");if(trackedNode)trackedNode.textContent=fmtInt(trackedWallets);const trackedMeta=el("lt-summary-tracked-meta");if(trackedMeta){trackedMeta.textContent="Leaderboard universe"}const portfolioNode=el("lt-summary-portfolio");if(portfolioNode){portfolioNode.textContent=Number.isFinite(portfolioCoveragePct)?fmtPct(portfolioCoveragePct,1):fmtInt(walletsWithPortfolioMetrics)}const portfolioMeta=el("lt-summary-portfolio-meta");if(portfolioMeta){portfolioMeta.textContent=`${fmtInt(walletsWithPortfolioMetrics)} wallets enriched`}const activeWalletsNode=el("lt-summary-active-wallets");if(activeWalletsNode)activeWalletsNode.textContent=fmtInt(liveActiveWalletsTotal);const activeWalletsMeta=el("lt-summary-active-wallets-meta");if(activeWalletsMeta){const liveGeneratedAt=tsMs(summary.liveGeneratedAt||(payload===null||payload===void 0?void 0:payload.generatedAt)||0);activeWalletsMeta.textContent=liveGeneratedAt?`Snapshot ${fmtAgo(liveGeneratedAt)}`:"Wallets with live positions"}const openPositionsNode=el("lt-summary-open-positions");if(openPositionsNode)openPositionsNode.textContent=fmtInt(openPositionsTotal);const openPositionsMeta=el("lt-summary-open-positions-meta");if(openPositionsMeta){const recentOpenRows=extractPositions(payload).length;openPositionsMeta.textContent=`${fmtInt(recentOpenRows)} rows in current page`}}function renderObservability(payload){var _payload$summary1;const obs=asObject(payload===null||payload===void 0||(_payload$summary1=payload.summary)===null||_payload$summary1===void 0?void 0:_payload$summary1.observability);const hot=asObject(obs.hotTier);const reconciliation=asObject(obs.reconciliation);const publicCoverage=asObject(obs.publicActiveCoverage);const hotValue=el("lt-obs-hot-value");if(hotValue){const active=n(hot.activeWallets,0);const capacity=n(hot.capacity,0);hotValue.textContent=capacity>0?`${fmtInt(active)} / ${fmtInt(capacity)}`:`${fmtInt(active)}`}const hotMeta=el("lt-obs-hot-meta");if(hotMeta){hotMeta.textContent=`${fmtPct(hot.utilizationPct,0)} util • dropped ${fmtInt(hot.droppedPromotions)}`}const triggerValue=el("lt-obs-trigger-value");if(triggerValue){const avg=n(hot.triggerToEventAvgMs,NaN);triggerValue.textContent=Number.isFinite(avg)&&avg>0?`${fmtInt(avg)} ms`:"-"}const triggerMeta=el("lt-obs-trigger-meta");if(triggerMeta){const last=n(hot.triggerToEventLastMs,NaN);triggerMeta.textContent=Number.isFinite(last)&&last>0?`last ${fmtInt(last)} ms`:"awaiting trigger traffic"}const coverageValue=el("lt-obs-coverage-value");if(coverageValue){const pct=n(publicCoverage.activeCoveragePct,NaN);coverageValue.textContent=Number.isFinite(pct)?fmtPct(pct,2):"-"}const coverageMeta=el("lt-obs-coverage-meta");if(coverageMeta){var _payload$summary10;const tracked=n(payload===null||payload===void 0||(_payload$summary10=payload.summary)===null||_payload$summary10===void 0||(_payload$summary10=_payload$summary10.walletFirstLive)===null||_payload$summary10===void 0?void 0:_payload$summary10.walletsWithOpenPositions,NaN);const lowerBound=n(publicCoverage.activeWalletsLowerBound,NaN);if(Number.isFinite(tracked)&&Number.isFinite(lowerBound)&&lowerBound>0){coverageMeta.textContent=`${fmtInt(tracked)} / ${fmtInt(lowerBound)} active wallets`}else{coverageMeta.textContent="public lower bound unavailable"}}const reconcileValue=el("lt-obs-reconcile-value");if(reconcileValue){const sweep=n(reconciliation.estimatedSweepSeconds,NaN);reconcileValue.textContent=Number.isFinite(sweep)&&sweep>0?`${fmtInt(sweep)}s sweep`:"-"}const reconcileMeta=el("lt-obs-reconcile-meta");if(reconcileMeta){const hotLoop=n(reconciliation.estimatedHotLoopSeconds,NaN);const lagMs=n(reconciliation.reconciliationLagMs,NaN);const parts=[];if(Number.isFinite(hotLoop)&&hotLoop>0)parts.push(`hot ${fmtInt(hotLoop)}s`);if(Number.isFinite(lagMs)&&lagMs>=0)parts.push(`lag ${fmtInt(lagMs)} ms`);reconcileMeta.textContent=parts.length?parts.join(" \u2022 "):"reconciliation warming"}}function renderWalletSupportNote(payload){const note=el("lt-wallet-support-note");if(!note)return;const support=extractWalletPerformanceSupport(payload);const available=safeArray(support.available);const unavailable=safeArray(support.unavailable);if(!available.length&&!unavailable.length){note.hidden=true;note.innerHTML="";return}const availableLabels=available.filter(entry=>["recent_trade_count","recent_volume","realized_pnl","unrealized_pnl","open_positions","exposure","symbol_concentration","live_active_score","freshness_state","wallet_holding_time","position_change_history"].includes(String(entry.key||""))).map(entry=>String(entry.label||"").trim()).filter(Boolean);const missingLabels=unavailable.slice(0,3).map(entry=>String(entry.label||"").trim()).filter(Boolean);note.hidden=false;note.innerHTML=`
    <div class="lt-wallet-support-copy">
      <strong>Backed by live activity and 24H / 7D / 30D / all-time wallet aggregates.</strong>
      <span>Primary surface only shows metrics with real support: ${escapeHtml(availableLabels.slice(0,5).join(" \u2022 "))}.</span>
    </div>
    ${missingLabels.length?`<div class="lt-wallet-support-muted">Hidden until a real dataset exists: ${escapeHtml(missingLabels.join(" \u2022 "))}.</div>`:""}
  `}function renderWalletRankingGroups(rows){var _groups$;const totalNode=el("lt-live-active-total");const list=el("lt-wallet-groups-grid");if(!list)return{groups:[],activeGroup:null,activeRows:rows};const groups=buildWalletRankingGroups(rows);const activeGroupKey=groups.some(group=>group.key===state.activeWalletGroup)?state.activeWalletGroup:groups.some(group=>group.key==="pnl")?"pnl":((_groups$=groups[0])===null||_groups$===void 0?void 0:_groups$.key)||"";state.activeWalletGroup=activeGroupKey;const activeGroup=groups.find(group=>group.key===activeGroupKey)||null;if(totalNode){totalNode.textContent=activeGroup?`${fmtInt(activeGroup.count)} wallets`:`${fmtInt(rows.length)} ranked wallets`}if(!groups.length){list.innerHTML=`<div class="lt-live-active-empty">No wallet groups match the current filter set.</div>`;return{groups:[],activeGroup:null,activeRows:rows}}list.innerHTML=groups.map(group=>{const selected=group.key===activeGroupKey;return`<button class="lt-wallet-group-card${selected?" is-active":""}" type="button" data-wallet-group="${escapeHtml(group.key)}" aria-pressed="${selected?"true":"false"}">
        <div class="lt-wallet-group-head">
          <div>
            <h4>${escapeHtml(group.title)}</h4>
            <p>${escapeHtml(group.subtitle)}</p>
          </div>
          ${badge(group.tone||"cool",`${fmtInt(group.count)} ranked`)}
        </div>
        <div class="lt-wallet-group-summary">
          <span class="lt-wallet-group-selection">${selected?"Selected":"Select group"}</span>
          <span class="lt-wallet-group-preview">${activeGroupKey===group.key&&group.rankedRows[0]?escapeHtml(walletGroupMetric(group.key,group.rankedRows[0]).primary):escapeHtml(group.subtitle)}</span>
        </div>
      </button>`}).join("");list.querySelectorAll("button[data-wallet-group]").forEach(btn=>{btn.addEventListener("click",()=>{const nextKey=String(btn.getAttribute("data-wallet-group")||"").trim();if(!nextKey||nextKey===state.activeWalletGroup)return;state.activeWalletGroup=nextKey;resetPagination();render()})});return{groups,activeGroup,activeRows:activeGroup?activeGroup.rankedRows:rows}}function payloadSignature(payload){const positionsTotal=payloadPositionEventsTotal(payload);const indexedWalletsTotal=payloadWalletsTotal(payload);const publicTradesLen=extractPublicTrades(payload).length;const positionsLen=extractPositions(payload).length;const walletPerfLen=extractWalletPerformance(payload).length;return[positionsTotal,indexedWalletsTotal,publicTradesLen,positionsLen,walletPerfLen].join(":")}function render(options={}){const refreshOptions=Boolean(options.refreshOptions);const payload=state.payload;if(!payload)return;const rows=state.rows||{positions:[],wallets:[],liveActive:[]};if(refreshOptions){refreshFilterOptions(rows)}const positionSourceRows=safeArray(rows.positions).length?rows.positions:extractPositions(payload);const positionsFiltered=applyPositionFilters(positionSourceRows);const positionsWindow=pageWindowFromSummary("positions",positionsFiltered.length);renderHealth(payload);renderPositionEventSummary(payload);renderPositionsTable(positionsFiltered,payload);renderPagination("positions",positionsWindow);updateToolbarControls()}async function fetchSnapshot(){var _state$pagination;if(state.activeView!=="positions"){return fetchJsonWithTimeout(buildLiveTradesPath("/api/live-trades"),SNAPSHOT_TIMEOUT_MS)}const positionsPageSize=Math.max(50,Math.min(500,Math.floor(n((_state$pagination=state.pagination)===null||_state$pagination===void 0||(_state$pagination=_state$pagination.positions)===null||_state$pagination===void 0?void 0:_state$pagination.pageSize,100))));const eventsPayload=await fetchJsonWithTimeout(buildLiveTradesPositionsPath("events",{all:false,positionLimit:positionsPageSize}),POSITIONS_FEED_TIMEOUT_MS);return mergeLiveTradePayloads(state.payload||{},eventsPayload,null)}function applyPayload(payload,source="poll"){var _payload$summary11,_payload$summary12,_payload$summary13;state.payload=payload;state.stream.source=source;state.stream.lastPayloadAt=Date.now();state.stream.lastSnapshotAt=Date.now();state.stream.lastLiveEventAt=tsMs((payload===null||payload===void 0||(_payload$summary11=payload.summary)===null||_payload$summary11===void 0||(_payload$summary11=_payload$summary11.walletFirstLive)===null||_payload$summary11===void 0?void 0:_payload$summary11.lastEventAt)||(payload===null||payload===void 0||(_payload$summary12=payload.summary)===null||_payload$summary12===void 0||(_payload$summary12=_payload$summary12.liveWalletFirst)===null||_payload$summary12===void 0?void 0:_payload$summary12.lastEventAt)||(payload===null||payload===void 0||(_payload$summary13=payload.summary)===null||_payload$summary13===void 0?void 0:_payload$summary13.liveGeneratedAt)||(payload===null||payload===void 0?void 0:payload.generatedAt)||0)||state.stream.lastLiveEventAt||Date.now();const nextPositionSignature=positionsSignature(payload);const nextWalletSignature=walletsSignature(payload);const nextLiveActiveSignature=liveActiveSignature(payload);const remapPositions=nextPositionSignature!==state.rowSignatures.positions;const remapWallets=nextWalletSignature!==state.rowSignatures.wallets;const remapLiveActive=nextLiveActiveSignature!==state.rowSignatures.liveActive;const nextSignature=payloadSignature(payload);const shouldRender=remapPositions||remapWallets||remapLiveActive||nextSignature!==state.payloadSignature;if(shouldRender){try{const nextRows=buildRows(payload,{includePositions:remapPositions,includeWallets:remapWallets,includeLiveActive:remapLiveActive});const prevRows=state.rows||{positions:[],wallets:[],liveActive:[]};const summary=(payload===null||payload===void 0?void 0:payload.summary)||{};const openPositionsTotal=Number.isFinite(n(summary.openPositionsTotal,NaN))?n(summary.openPositionsTotal,0):payloadPositionsTotal(payload);const indexedWalletsTotal=Number.isFinite(n(summary.indexedWalletsTotal,NaN))?n(summary.indexedWalletsTotal,0):payloadWalletsTotal(payload);const streamLikelyAlive=source==="sse"||state.stream.connection==="open";const preservePositions=remapPositions&&streamLikelyAlive&&safeArray(nextRows.positions).length===0&&prevRows.positions.length>0&&openPositionsTotal>0;const preserveWallets=remapWallets&&streamLikelyAlive&&safeArray(nextRows.wallets).length===0&&prevRows.wallets.length>0&&indexedWalletsTotal>0;const nextPositionRows=safeArray(nextRows.positions);const prevPositionKeySet=new Set(safeArray(prevRows.positions).map(row=>String((row===null||row===void 0?void 0:row.key)||"").trim()).filter(Boolean));const seenPositionKeys=state.positionsSeenKeys instanceof Set?new Set(state.positionsSeenKeys):new Set;const annotatedPositions=nextPositionRows.map(row=>{const key=String((row===null||row===void 0?void 0:row.key)||"").trim();if(!key)return row;const unseen=!seenPositionKeys.has(key)&&!prevPositionKeySet.has(key);seenPositionKeys.add(key);if(!unseen||source==="bootstrap")return row;return{...row,isNewlySeen:true}});state.positionsSeenKeys=seenPositionKeys;state.rows={positions:remapPositions?preservePositions?prevRows.positions:annotatedPositions:prevRows.positions,wallets:remapWallets?preserveWallets?prevRows.wallets:safeArray(nextRows.wallets):prevRows.wallets,liveActive:remapLiveActive?safeArray(nextRows.liveActive):prevRows.liveActive};if(remapPositions)state.rowSignatures.positions=nextPositionSignature;if(remapWallets)state.rowSignatures.wallets=nextWalletSignature;if(remapLiveActive)state.rowSignatures.liveActive=nextLiveActiveSignature;state.payloadSignature=nextSignature}catch(error){state.stream.lastError=`payload_map_error: ${error&&error.message?error.message:"unknown"}`;state.stream.lastErrorAt=Date.now()}}if(shouldRender||source!=="poll"){render({refreshOptions:true})}}async function refreshMetrics(source="metrics_poll"){try{const metrics=await fetchJsonWithTimeout(buildLiveTradesPath("/api/live-trades/metrics"),SNAPSHOT_TIMEOUT_MS);state.metrics=metrics;if(state.payload){renderPositionEventSummary(state.payload);renderHealth(state.payload)}}catch(error){state.stream.lastError=error&&error.message?error.message:"metrics_error";state.stream.lastErrorAt=Date.now()}}function stopEventSource(){if(state.stream.eventSource){state.stream.eventSource.close();state.stream.eventSource=null}}function ensureFallbackPolling(){if(state.stream.fallbackTimer)return;state.stream.fallbackTimer=setInterval(async()=>{if(state.stream.paused)return;try{const payload=await fetchSnapshot();applyPayload(payload,"poll");if(state.stream.connection!=="open")state.stream.connection="fallback"}catch(error){state.stream.lastError=error.message;state.stream.lastErrorAt=Date.now()}},1000)}async function refreshPositionsFeed(source="positions_poll"){var _state$pagination2;if(state.stream.positionsRequestInFlight){state.stream.positionsRefreshQueued=true;return}state.stream.positionsRequestInFlight=true;state.stream.positionsRefreshQueued=false;const requestSeq=Number(state.stream.positionsRequestSeq||0)+1;state.stream.positionsRequestSeq=requestSeq;const positionsPageSize=Math.max(50,Math.min(500,Math.floor(n((_state$pagination2=state.pagination)===null||_state$pagination2===void 0||(_state$pagination2=_state$pagination2.positions)===null||_state$pagination2===void 0?void 0:_state$pagination2.pageSize,100))));try{var _eventsPayload$summar,_eventsPayload$summar2;const eventsPayload=await fetchJsonWithTimeout(buildLiveTradesPositionsPath("events",{all:false,positionLimit:positionsPageSize}),POSITIONS_FEED_TIMEOUT_MS);const payloadGeneratedAt=tsMs((eventsPayload===null||eventsPayload===void 0?void 0:eventsPayload.generatedAt)||(eventsPayload===null||eventsPayload===void 0||(_eventsPayload$summar=eventsPayload.summary)===null||_eventsPayload$summar===void 0?void 0:_eventsPayload$summar.liveGeneratedAt)||(eventsPayload===null||eventsPayload===void 0||(_eventsPayload$summar2=eventsPayload.summary)===null||_eventsPayload$summar2===void 0||(_eventsPayload$summar2=_eventsPayload$summar2.walletFirstLive)===null||_eventsPayload$summar2===void 0?void 0:_eventsPayload$summar2.lastEventAt)||0);const lastAppliedGeneratedAt=n(state.stream.positionsLastAppliedGeneratedAt,0);const lastAppliedSeq=n(state.stream.positionsAppliedSeq,0);if(requestSeq<lastAppliedSeq||payloadGeneratedAt>0&&lastAppliedGeneratedAt>0&&payloadGeneratedAt+1<lastAppliedGeneratedAt){return}const nextPayload=mergeLiveTradePayloads(state.payload||{},eventsPayload,null);applyPayload(nextPayload,source);state.stream.positionsAppliedSeq=requestSeq;if(payloadGeneratedAt>0){state.stream.positionsLastAppliedGeneratedAt=Math.max(lastAppliedGeneratedAt,payloadGeneratedAt)}}finally{state.stream.positionsRequestInFlight=false;if(state.stream.positionsRefreshQueued&&!state.stream.paused&&state.activeView==="positions"){state.stream.positionsRefreshQueued=false;setTimeout(()=>{refreshPositionsFeed(`${source}_queued`).catch(error=>{state.stream.lastError=error&&error.message?error.message:"positions_feed_error";state.stream.lastErrorAt=Date.now()})},0)}}}function ensurePositionsPolling(){if(state.stream.positionsTimer)return;state.stream.positionsTimer=setInterval(()=>{if(state.stream.paused||state.activeView!=="positions")return;refreshPositionsFeed().catch(error=>{state.stream.lastError=error&&error.message?error.message:"positions_feed_error";state.stream.lastErrorAt=Date.now()})},POSITIONS_FEED_POLL_MS)}function clearPositionsPolling(){if(state.stream.positionsTimer){clearInterval(state.stream.positionsTimer);state.stream.positionsTimer=null}}function clearFallbackPolling(){if(state.stream.fallbackTimer){clearInterval(state.stream.fallbackTimer);state.stream.fallbackTimer=null}}function ensureMetricsPolling(){if(state.stream.metricsTimer)return;state.stream.metricsTimer=setInterval(()=>{if(state.stream.paused)return;refreshMetrics().catch(()=>{})},5000)}function clearMetricsPolling(){if(state.stream.metricsTimer){clearInterval(state.stream.metricsTimer);state.stream.metricsTimer=null}}function connectSse(){if(state.stream.paused)return;stopEventSource();state.stream.connection="connecting";const streamPath=buildLiveTradesPath("/api/live-trades/stream",{cacheBust:Date.now()});const es=new EventSource(streamPath);state.stream.eventSource=es;const handleStreamMessage=event=>{if(!event||!event.data)return;try{const message=JSON.parse(event.data);if(message&&message.type==="heartbeat"){state.stream.lastHeartbeatAt=Date.now();renderHealth(state.payload||{});return}if(state.activeView==="positions"){var _message$payload,_message$payload2;state.stream.lastLiveEventAt=tsMs((message===null||message===void 0||(_message$payload=message.payload)===null||_message$payload===void 0||(_message$payload=_message$payload.summary)===null||_message$payload===void 0?void 0:_message$payload.liveGeneratedAt)||(message===null||message===void 0||(_message$payload2=message.payload)===null||_message$payload2===void 0?void 0:_message$payload2.generatedAt)||Date.now())||state.stream.lastLiveEventAt||Date.now();if(message&&message.payload&&message.payload.summary){var _state$metrics,_state$metrics2,_state$metrics3,_state$metrics4,_state$metrics5;state.metrics={...(asObject(state.metrics)||{}),generatedAt:tsMs(message.payload.generatedAt||Date.now())||Date.now(),aggregate:{...asObject((_state$metrics=state.metrics)===null||_state$metrics===void 0?void 0:_state$metrics.aggregate),trackedWallets:n(message.payload.summary.trackedWallets,n((_state$metrics2=state.metrics)===null||_state$metrics2===void 0||(_state$metrics2=_state$metrics2.aggregate)===null||_state$metrics2===void 0?void 0:_state$metrics2.trackedWallets,0)),openPositionsTotal:n(message.payload.summary.openPositionsTotal,n((_state$metrics3=state.metrics)===null||_state$metrics3===void 0||(_state$metrics3=_state$metrics3.aggregate)===null||_state$metrics3===void 0?void 0:_state$metrics3.openPositionsTotal,0)),newOpenEventsPerMinute:n(message.payload.summary.positionEventOpened1m,n((_state$metrics4=state.metrics)===null||_state$metrics4===void 0||(_state$metrics4=_state$metrics4.aggregate)===null||_state$metrics4===void 0?void 0:_state$metrics4.newOpenEventsPerMinute,0)),eventsPerMinute:n(message.payload.summary.positionEventsTotal,n((_state$metrics5=state.metrics)===null||_state$metrics5===void 0||(_state$metrics5=_state$metrics5.aggregate)===null||_state$metrics5===void 0?void 0:_state$metrics5.eventsPerMinute,0))}};renderPositionEventSummary(state.payload||{})}return}if(message&&message.type==="snapshot"&&message.payload){if(state.activeView==="positions"){var _message$payload3,_message$payload4;state.stream.lastSnapshotAt=Date.now();state.stream.lastLiveEventAt=tsMs((message===null||message===void 0||(_message$payload3=message.payload)===null||_message$payload3===void 0||(_message$payload3=_message$payload3.summary)===null||_message$payload3===void 0?void 0:_message$payload3.liveGeneratedAt)||(message===null||message===void 0||(_message$payload4=message.payload)===null||_message$payload4===void 0?void 0:_message$payload4.generatedAt)||Date.now())||state.stream.lastLiveEventAt||Date.now();renderHealth(state.payload||{});refreshPositionsFeed("positions_sse").catch(()=>{});return}applyPayload(message.payload,"sse");return}if(message&&message.payload){if(state.activeView==="positions"){var _message$payload5,_message$payload6;state.stream.lastLiveEventAt=tsMs((message===null||message===void 0||(_message$payload5=message.payload)===null||_message$payload5===void 0||(_message$payload5=_message$payload5.summary)===null||_message$payload5===void 0?void 0:_message$payload5.liveGeneratedAt)||(message===null||message===void 0||(_message$payload6=message.payload)===null||_message$payload6===void 0?void 0:_message$payload6.generatedAt)||Date.now())||state.stream.lastLiveEventAt||Date.now();renderHealth(state.payload||{});refreshPositionsFeed("positions_sse").catch(()=>{});return}applyPayload(message.payload,"sse")}}catch(_error){}};es.onopen=()=>{state.stream.connection="open";state.stream.retryMs=500;state.stream.lastError=null;clearFallbackPolling();render()};es.onmessage=handleStreamMessage;es.addEventListener("snapshot",handleStreamMessage);es.addEventListener("delta",handleStreamMessage);es.addEventListener("heartbeat",handleStreamMessage);es.onerror=()=>{state.stream.lastError="sse_error";state.stream.lastErrorAt=Date.now();state.stream.connection="reconnecting";render();stopEventSource();ensureFallbackPolling();const waitMs=state.stream.retryMs;setTimeout(()=>connectSse(),waitMs);state.stream.retryMs=Math.min(RECONNECT_MAX_MS,Math.round(waitMs*1.6))}}function startHealthLoop(){if(state.stream.healthTimer)return;state.stream.healthTimer=setInterval(()=>{renderHealth(state.payload||{})},1000)}function readNumber(id,options={}){var _el;const floorZero=options.floorZero!==false;const value=n((_el=el(id))===null||_el===void 0?void 0:_el.value,0);return floorZero?Math.max(0,value):value}function wireEvents(){var _el2,_el3,_el4,_el5,_el6,_el7,_el8,_el9,_el0,_el1,_el10,_el11,_el12,_el13,_el14,_el15,_el16,_el17,_el18;(_el2=el("lt-view-positions-btn"))===null||_el2===void 0||_el2.addEventListener("click",()=>{state.activeView="positions";if(state.selection.type&&state.selection.type!=="positions"){state.selection={type:"",key:""}}render();refreshPositionsFeed("positions_view").catch(()=>{})});(_el3=el("lt-view-wallets-btn"))===null||_el3===void 0||_el3.addEventListener("click",()=>{state.activeView="wallets";if(state.selection.type&&state.selection.type!=="wallets"){state.selection={type:"",key:""}}render()});(_el4=el("lt-detail-close"))===null||_el4===void 0||_el4.addEventListener("click",()=>{state.selection={type:"",key:""};closeWalletDrawer();render()});window.addEventListener("keydown",event=>{if(event.key==="Escape"&&(state.selection.key||state.drawer.wallet)){state.selection={type:"",key:""};closeWalletDrawer();render()}});(_el5=el("lt-filter-wallet"))===null||_el5===void 0||_el5.addEventListener("input",event=>{state.filters.wallet=String(event.target.value||"");resetPagination();render()});(_el6=el("lt-refresh-now-btn"))===null||_el6===void 0||_el6.addEventListener("click",()=>{refreshNow().catch(error=>{state.stream.lastError=(error===null||error===void 0?void 0:error.message)||"manual_refresh_error";state.stream.lastErrorAt=Date.now();renderHealth(state.payload||{})})});(_el7=el("lt-stream-toggle-btn"))===null||_el7===void 0||_el7.addEventListener("click",()=>{toggleStreamState()});(_el8=el("lt-filter-min-usd"))===null||_el8===void 0||_el8.addEventListener("input",()=>{state.filters.minUsd=readNumber("lt-filter-min-usd");resetPagination();render()});(_el9=el("lt-filter-max-usd"))===null||_el9===void 0||_el9.addEventListener("input",()=>{state.filters.maxUsd=readNumber("lt-filter-max-usd");resetPagination();render()});(_el0=el("lt-filter-min-pnl"))===null||_el0===void 0||_el0.addEventListener("input",()=>{state.filters.minPnl=readNumber("lt-filter-min-pnl",{floorZero:false});resetPagination();render()});(_el1=el("lt-filter-max-pnl"))===null||_el1===void 0||_el1.addEventListener("input",()=>{state.filters.maxPnl=readNumber("lt-filter-max-pnl",{floorZero:false});resetPagination();render()});(_el10=el("lt-filter-freshness"))===null||_el10===void 0||_el10.addEventListener("change",event=>{state.filters.freshness=String(event.target.value||"");resetPagination();render()});(_el11=el("lt-filter-tracked"))===null||_el11===void 0||_el11.addEventListener("change",event=>{state.filters.trackedOnly=Boolean(event.target.checked);resetPagination();render()});(_el12=el("lt-positions-page-size"))===null||_el12===void 0||_el12.addEventListener("change",event=>{state.pagination.positions.pageSize=Math.max(10,Math.floor(n(event.target.value,100)));state.pagination.positions.page=1;fetchSnapshot().then(payload=>applyPayload(payload,"page")).catch(()=>{});connectSse()});(_el13=el("lt-wallets-page-size"))===null||_el13===void 0||_el13.addEventListener("change",event=>{state.pagination.wallets.pageSize=Math.max(10,Math.floor(n(event.target.value,100)));state.pagination.wallets.page=1;fetchSnapshot().then(payload=>applyPayload(payload,"page")).catch(()=>{});connectSse()});(_el14=el("lt-positions-page-prev"))===null||_el14===void 0||_el14.addEventListener("click",()=>{state.pagination.positions.page=Math.max(1,n(state.pagination.positions.page,1)-1);fetchSnapshot().then(payload=>applyPayload(payload,"page")).catch(()=>{});connectSse()});(_el15=el("lt-positions-page-next"))===null||_el15===void 0||_el15.addEventListener("click",()=>{state.pagination.positions.page=n(state.pagination.positions.page,1)+1;fetchSnapshot().then(payload=>applyPayload(payload,"page")).catch(()=>{});connectSse()});(_el16=el("lt-wallets-page-prev"))===null||_el16===void 0||_el16.addEventListener("click",()=>{state.pagination.wallets.page=Math.max(1,n(state.pagination.wallets.page,1)-1);fetchSnapshot().then(payload=>applyPayload(payload,"page")).catch(()=>{});connectSse()});(_el17=el("lt-wallets-page-next"))===null||_el17===void 0||_el17.addEventListener("click",()=>{state.pagination.wallets.page=n(state.pagination.wallets.page,1)+1;fetchSnapshot().then(payload=>applyPayload(payload,"page")).catch(()=>{});connectSse()});(_el18=el("lt-reset-filters"))===null||_el18===void 0||_el18.addEventListener("click",()=>{state.search="";state.filters={wallet:"",minUsd:0,maxUsd:0,minPnl:0,maxPnl:0,freshness:"",trackedOnly:false};["lt-filter-wallet","lt-filter-min-usd","lt-filter-max-usd","lt-filter-min-pnl","lt-filter-max-pnl"].forEach(id=>{if(el(id))el(id).value=""});["lt-filter-freshness"].forEach(id=>{if(el(id))el(id).value=""});if(el("lt-filter-tracked"))el("lt-filter-tracked").checked=false;resetPagination();render()})}async function boot(){wireEvents();startHealthLoop();state.stream.connection="connecting";renderHealth(state.payload||{});connectSse();ensureFallbackPolling();ensurePositionsPolling();ensureMetricsPolling();try{const payload=await fetchSnapshot();applyPayload(payload,"bootstrap");refreshMetrics("bootstrap").catch(()=>{})}catch(_error){}if(typeof window!=="undefined"){window.__PF_LIVE_TRADE_BOOTED=true}}boot();
